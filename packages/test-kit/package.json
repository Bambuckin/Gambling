{
  "name": "@lottery/test-kit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "node -e \"console.log('test-kit lint is not configured yet')\"",
    "typecheck": "tsc -p tsconfig.json --pretty false",
    "test": "node -e \"console.log('test-kit tests are not configured yet')\"",
    "smoke": "node -e \"console.log('test-kit smoke scaffold ready')\""
  },
  "dependencies": {
    "@lottery/application": "workspace:*",
    "@lottery/domain": "workspace:*",
    "@lottery/infrastructure": "workspace:*",
    "@lottery/lottery-handlers": "workspace:*"
  }
}
