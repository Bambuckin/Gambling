# Runbooks

This directory will hold operator-facing procedures.

Current runbooks:

- `local-bootstrap.md` - how to verify git, planning state, and the correct Phase 1 entrypoint before implementation resumes

Expected runbooks after Phase 1:

- local bootstrap
- fake terminal smoke flow
- queue incident triage
- terminal degraded/offline recovery
- adding or updating a lottery handler
- validating balance/event consistency during failures

If a later phase introduces operational behavior without a runbook update, that phase is incomplete.
