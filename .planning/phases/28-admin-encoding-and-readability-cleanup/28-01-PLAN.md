---
phase: 28
plan: 28-01
title: Admin encoding and visible status presenter
status: completed
wave: 1
depends_on: [27-01]
files_modified:
  - apps/web/src/app/admin/page.tsx
  - apps/web/src/lib/purchase/admin-draw-monitor.tsx
  - apps/web/src/lib/purchase/admin-status-presenter.ts
  - apps/web/src/lib/purchase/__tests__/admin-status-presenter.test.ts
---

<objective>
Remove corrupted and mixed-language admin UI labels, and centralize admin-visible status/reference mapping so touched JSX does not expose raw backend states.
</objective>

<tasks>
  <task id="1" name="Create admin status presenter">
    <read_first>
      - apps/web/src/app/admin/page.tsx
      - apps/web/src/lib/purchase/admin-draw-monitor.tsx
      - packages/application/src/services/admin-operations-query-service.ts
      - packages/application/src/services/terminal-receiver-query-service.ts
    </read_first>
    <action>
      Add `apps/web/src/lib/purchase/admin-status-presenter.ts` with Russian label helpers for terminal state, queue status, receiver state, cash-desk status, winnings credit status, alert severity/content, audit domain/action/severity/target/reference, draw status, verification status, admin mark, and ticket outcome. Unknown status fallbacks must return safe Russian labels such as `Статус уточняется` instead of the raw input value.
    </action>
    <acceptance_criteria>
      - `admin-status-presenter.ts` exports functions used by both admin page and draw monitor.
      - No presenter status helper returns the raw unknown status string.
      - Audit reference output uses `Заявка`, `Пользователь`, `Лотерея`, `Тираж`, `Терминал`, and `Запись баланса` labels.
    </acceptance_criteria>
  </task>

  <task id="2" name="Replace corrupted admin UI copy">
    <read_first>
      - apps/web/src/app/admin/page.tsx
      - apps/web/src/lib/purchase/admin-draw-monitor.tsx
    </read_first>
    <action>
      Update admin JSX to import the presenter helpers, remove local scattered status formatters, replace `????` close/settle messages, and replace visible mixed English labels such as `settlement`, `legacy`, `audit-`, and `request=` in touched sections with Russian operator text.
    </action>
    <acceptance_criteria>
      - `Select-String` over the touched admin files finds no `????`.
      - Visible touched admin copy no longer contains `legacy`, `audit-`, `request=`, `user=`, `lottery=`, `draw=`, `terminal=`, or `ledger=` labels; JSX prop syntax is not part of this scan.
      - `apps/web/src/app/admin/page.tsx` no longer contains local `formatQueueStatus`, `formatRequestState`, `formatAlertSeverity`, `formatAuditSeverity`, or `formatOperationsReference` helpers.
    </acceptance_criteria>
  </task>

  <task id="3" name="Validate focused behavior">
    <read_first>
      - apps/web/src/lib/purchase/__tests__/lottery-live-request-presenter.test.ts
      - apps/web/src/lib/purchase/__tests__/lottery-live-ticket-presenter.test.ts
    </read_first>
    <action>
      Add focused Vitest coverage for the new admin presenter and run the smallest relevant web test command, then web typecheck.
    </action>
    <acceptance_criteria>
      - `corepack pnpm --filter @lottery/web test -- src/lib/purchase/__tests__/admin-status-presenter.test.ts` passes.
      - `corepack pnpm --filter @lottery/web typecheck` passes.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- Focused web presenter test.
- Web typecheck.
- Targeted string scan for corrupted/mixed admin labels in touched files.
</verification>

<must_haves>
- Receiver/latest-request states render as Russian labels.
- Touched admin status helpers use a single presenter layer instead of raw JSX fallback.
- Corrupted close/settle draw messages are replaced with real Russian strings.
- Audit-style references no longer use English `key=value` labels.
</must_haves>
