---
phase: 05-purchase-request-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/domain/src/purchase-draft.ts
  - packages/domain/src/__tests__/purchase-draft.test.ts
  - packages/domain/src/index.ts
  - packages/application/src/services/purchase-draft-service.ts
  - packages/application/src/__tests__/purchase-draft-service.test.ts
  - packages/application/src/index.ts
  - apps/web/src/app/lottery/[lotteryCode]/page.tsx
autonomous: true
requirements: [PURC-01, PURC-02]
user_setup: []
must_haves:
  truths:
    - registry field metadata is the only validation source
    - pricing quote is deterministic for the same lottery input
  artifacts:
    - packages/domain/src/purchase-draft.ts
    - packages/application/src/services/purchase-draft-service.ts
    - packages/application/src/__tests__/purchase-draft-service.test.ts
  key_links:
    - apps/web/src/app/lottery/[lotteryCode]/page.tsx
---

<objective>
Implement lottery-specific field validation and deterministic pricing quote pipeline before request confirmation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-purchase-request-orchestration/05-CONTEXT.md
@.planning/REQUIREMENTS.md
@apps/web/src/app/lottery/[lotteryCode]/page.tsx
@packages/domain/src/lottery-registry.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add domain-level purchase draft validation and quote model</name>
  <files>packages/domain/src/purchase-draft.ts, packages/domain/src/__tests__/purchase-draft.test.ts, packages/domain/src/index.ts</files>
  <read_first>packages/domain/src/lottery-registry.ts, packages/domain/src/request-state.ts</read_first>
  <action>Create domain helpers that validate required/optional field values by `LotteryFormFieldDefinition` and return structured field errors by `fieldKey`. Implement deterministic quote calculator that supports current `fixed` strategy and applies `draw_count` multiplier when present. Reject unsupported pricing strategies (`matrix`, `formula`) with explicit domain validation errors so the service cannot silently price wrong values.</action>
  <verify>corepack pnpm --filter @lottery/domain test</verify>
  <acceptance_criteria>
    - `packages/domain/src/purchase-draft.ts` exports validation and quote helpers plus typed error result
    - tests cover missing required field, select option mismatch, numeric range violation, and fixed pricing quote
    - `packages/domain/src/index.ts` re-exports purchase draft contracts
  </acceptance_criteria>
  <done>Domain package can validate lottery payload and produce deterministic quote values.</done>
</task>

<task type="auto">
  <name>Task 2: Add application purchase draft service boundary</name>
  <files>packages/application/src/services/purchase-draft-service.ts, packages/application/src/__tests__/purchase-draft-service.test.ts, packages/application/src/index.ts</files>
  <read_first>packages/application/src/services/lottery-registry-service.ts, packages/application/src/services/draw-refresh-service.ts</read_first>
  <action>Implement `PurchaseDraftService` that resolves lottery by code from `LotteryRegistryService`, validates payload through domain helper, and returns quote DTO with `lotteryCode`, `validatedPayload`, `costMinor`, and `currency`. Fail with typed application error when lottery is missing, disabled, or payload is invalid. Keep service read-only: no request store writes and no ledger mutations in this plan.</action>
  <verify>corepack pnpm --filter @lottery/application test</verify>
  <acceptance_criteria>
    - service exposes one method for draft validation+quote without side effects
    - application tests assert success path and invalid payload path with field error details
    - application index exports new service and error type
  </acceptance_criteria>
  <done>Application layer has a reusable purchase draft quote pipeline for web and future request creation flow.</done>
</task>

<task type="auto">
  <name>Task 3: Wire lottery page draft action to validation and pricing quote</name>
  <files>apps/web/src/app/lottery/[lotteryCode]/page.tsx</files>
  <read_first>apps/web/src/app/lottery/[lotteryCode]/page.tsx, apps/web/src/lib/registry/registry-runtime.ts</read_first>
  <action>Replace current "captured fields count" response in `submitPurchaseDraftAction` with `PurchaseDraftService` call. Build payload map from submitted form fields, pass to service, and redirect with message including `costMinor`, currency, and validated field count. On validation failure, redirect with explicit error message that includes the first invalid field key. Keep draw freshness gate intact and do not create requests yet.</action>
  <verify>corepack pnpm --filter @lottery/web build</verify>
  <acceptance_criteria>
    - submit action no longer reports only captured count; it reports priced quote
    - invalid payload redirects with `draft=error` and field-specific reason
    - stale/missing draw still blocks action before validation/quote path
  </acceptance_criteria>
  <done>User draft submission now runs real validation and pricing before confirmation phase.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm --filter @lottery/domain test` passes
- [ ] `corepack pnpm --filter @lottery/application test` passes
- [ ] `corepack pnpm --filter @lottery/web build` passes
- [ ] Lottery draft message shows `cost` and `currency`
</verification>

<output>
After completion, create `.planning/phases/05-purchase-request-orchestration/05-01-SUMMARY.md`
</output>
