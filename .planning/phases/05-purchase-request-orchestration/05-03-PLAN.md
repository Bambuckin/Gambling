---
phase: 05-purchase-request-orchestration
plan: 03
type: execute
wave: 2
depends_on:
  - 05-02
files_modified:
  - packages/application/src/ports/purchase-queue-store.ts
  - packages/application/src/services/purchase-orchestration-service.ts
  - packages/application/src/__tests__/purchase-orchestration-service.test.ts
  - packages/infrastructure/src/purchase/in-memory-purchase-queue-store.ts
  - apps/web/src/lib/purchase/purchase-runtime.ts
  - apps/web/src/app/lottery/[lotteryCode]/page.tsx
autonomous: true
requirements: [PURC-04]
user_setup: []
must_haves:
  truths:
    - confirmed request performs reserve and queue insert as one orchestration path
    - reserve and queue writes are idempotent per request id
  artifacts:
    - packages/application/src/services/purchase-orchestration-service.ts
    - packages/application/src/ports/purchase-queue-store.ts
    - packages/application/src/__tests__/purchase-orchestration-service.test.ts
  key_links:
    - apps/web/src/lib/purchase/purchase-runtime.ts
---

<objective>
Connect confirmed request to ledger reserve and queue insertion with idempotent orchestration boundaries.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-purchase-request-orchestration/05-CONTEXT.md
@.planning/phases/05-purchase-request-orchestration/05-02-PLAN.md
@packages/application/src/services/wallet-ledger-service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add queue port and in-memory adapter for purchase requests</name>
  <files>packages/application/src/ports/purchase-queue-store.ts, packages/infrastructure/src/purchase/in-memory-purchase-queue-store.ts, packages/infrastructure/src/index.ts, packages/application/src/index.ts</files>
  <read_first>packages/application/src/ports/queue.ts, packages/infrastructure/src/ledger/in-memory-ledger-store.ts</read_first>
  <action>Create queue store contract for enqueue/list operations with deterministic queue item shape (`requestId`, `priority`, `enqueuedAt`, `attemptCount`, `status`). Implement in-memory adapter with append-only queue history and idempotent enqueue by `requestId` so repeated confirm calls do not duplicate queued items.</action>
  <verify>corepack pnpm --filter @lottery/infrastructure typecheck</verify>
  <acceptance_criteria>
    - queue port and adapter are exported and compilable
    - enqueue ignores duplicate `requestId` while returning existing queued item
    - queue list order is stable by enqueue timestamp then request id
  </acceptance_criteria>
  <done>Infrastructure can hold queue data for request orchestration flow.</done>
</task>

<task type="auto">
  <name>Task 2: Implement purchase orchestration service for reserve + queue insert</name>
  <files>packages/application/src/services/purchase-orchestration-service.ts, packages/application/src/__tests__/purchase-orchestration-service.test.ts</files>
  <read_first>packages/application/src/services/purchase-request-service.ts, packages/application/src/services/wallet-ledger-service.ts, packages/domain/src/request-state.ts</read_first>
  <action>Implement service method `confirmAndQueueRequest` that loads awaiting-confirmation request snapshot, reserves funds via `WalletLedgerService.reserveFunds`, transitions request journal to `confirmed` then `queued`, and enqueues request in queue store. Make all side effects idempotent by requestId + phase-specific keys (`{requestId}:reserve`, `{requestId}:enqueue`).</action>
  <verify>corepack pnpm --filter @lottery/application test</verify>
  <acceptance_criteria>
    - confirmed request creates exactly one reserve entry and one queue item
    - replayed confirmation call returns existing orchestration result without duplicate side effects
    - tests cover insufficient funds path and queue idempotency path
  </acceptance_criteria>
  <done>Application service now creates execution-ready queued requests with reserved funds.</done>
</task>

<task type="auto">
  <name>Task 3: Wire runtime and web confirm action to orchestration service</name>
  <files>apps/web/src/lib/purchase/purchase-runtime.ts, apps/web/src/app/lottery/[lotteryCode]/page.tsx</files>
  <read_first>apps/web/src/lib/ledger/ledger-runtime.ts, apps/web/src/lib/registry/registry-runtime.ts, apps/web/src/app/lottery/[lotteryCode]/page.tsx</read_first>
  <action>Add purchase runtime composition that wires request store, queue store, and wallet ledger service. Update confirmation action to call orchestration service instead of creating snapshot-only records, and report request id + queued status in response message.</action>
  <verify>corepack pnpm --filter @lottery/web build</verify>
  <acceptance_criteria>
    - confirm action response includes queued request identifier
    - reserve and queue insertion happen through runtime service (not route-local logic)
    - web build succeeds with new purchase runtime wiring
  </acceptance_criteria>
  <done>UI confirmation path now moves request into queued state with reserved funds.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm --filter @lottery/application test` passes
- [ ] `corepack pnpm --filter @lottery/infrastructure typecheck` passes
- [ ] `corepack pnpm --filter @lottery/web build` passes
</verification>

<output>
After completion, create `.planning/phases/05-purchase-request-orchestration/05-03-SUMMARY.md`
</output>
