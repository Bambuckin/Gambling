---
phase: 05-purchase-request-orchestration
plan: 04
type: execute
wave: 2
depends_on:
  - 05-03
files_modified:
  - packages/domain/src/request-state.ts
  - packages/application/src/services/purchase-request-service.ts
  - packages/application/src/services/purchase-orchestration-service.ts
  - packages/application/src/__tests__/purchase-orchestration-service.test.ts
  - apps/web/src/app/lottery/[lotteryCode]/page.tsx
autonomous: true
requirements: [PURC-05]
user_setup: []
must_haves:
  truths:
    - queued and retrying requests are cancelable; executing and final states are not
    - cancellation releases reserve exactly once
  artifacts:
    - packages/application/src/services/purchase-orchestration-service.ts
    - packages/application/src/__tests__/purchase-orchestration-service.test.ts
  key_links:
    - packages/domain/src/request-state.ts
---

<objective>
Implement cancelability rules and request lifecycle transitions needed before terminal execution starts.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-purchase-request-orchestration/05-CONTEXT.md
@.planning/phases/05-purchase-request-orchestration/05-03-PLAN.md
@packages/domain/src/request-state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend lifecycle rules for explicit cancelability checks</name>
  <files>packages/domain/src/request-state.ts, packages/domain/src/__tests__/request-state.test.ts</files>
  <read_first>packages/domain/src/request-state.ts</read_first>
  <action>Add helper predicates `canCancelRequestState` and `assertCancelableState` that allow cancellation only for `queued` and `retrying` states, and reject `executing`, `success`, `error`, and `reserve_released`. Add unit tests that prove allowed and blocked cancellation states.</action>
  <verify>corepack pnpm --filter @lottery/domain test</verify>
  <acceptance_criteria>
    - request-state module exports cancelability helpers
    - tests cover all non-terminal and terminal states for cancellation behavior
    - blocked state error message includes current state string
  </acceptance_criteria>
  <done>Domain layer has explicit cancelability contract.</done>
</task>

<task type="auto">
  <name>Task 2: Implement cancel request command with reserve release</name>
  <files>packages/application/src/services/purchase-request-service.ts, packages/application/src/services/purchase-orchestration-service.ts, packages/application/src/__tests__/purchase-orchestration-service.test.ts</files>
  <read_first>packages/application/src/services/purchase-orchestration-service.ts, packages/application/src/services/wallet-ledger-service.ts</read_first>
  <action>Add `cancelQueuedRequest` flow: load request, validate cancelable state, append `canceled` journal event, release reserved funds with idempotency key `{requestId}:cancel-release`, and append `reserve_released` event. Ensure repeated cancel command is idempotent and does not release funds twice.</action>
  <verify>corepack pnpm --filter @lottery/application test</verify>
  <acceptance_criteria>
    - cancel command works for queued/retrying requests
    - cancel command rejects executing and final states with explicit reason
    - reserve release ledger entry appears exactly once for repeated cancel call
  </acceptance_criteria>
  <done>Application orchestration supports safe cancellation and reserve release.</done>
</task>

<task type="auto">
  <name>Task 3: Add web cancellation action for queued requests</name>
  <files>apps/web/src/app/lottery/[lotteryCode]/page.tsx</files>
  <read_first>apps/web/src/app/lottery/[lotteryCode]/page.tsx</read_first>
  <action>Add server action endpoint for request cancellation and render cancel controls only for cancelable statuses in the current request list section. Redirect with success/error message including requestId and resulting state.</action>
  <verify>corepack pnpm --filter @lottery/web build</verify>
  <acceptance_criteria>
    - cancel control not shown for non-cancelable request states
    - cancel action redirects with clear status message
    - web build passes with cancellation flow wiring
  </acceptance_criteria>
  <done>User can cancel eligible queued requests before terminal execution starts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm --filter @lottery/domain test` passes
- [ ] `corepack pnpm --filter @lottery/application test` passes
- [ ] `corepack pnpm --filter @lottery/web build` passes
</verification>

<output>
After completion, create `.planning/phases/05-purchase-request-orchestration/05-04-SUMMARY.md`
</output>
