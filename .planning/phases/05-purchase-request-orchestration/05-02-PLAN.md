---
phase: 05-purchase-request-orchestration
plan: 02
type: execute
wave: 1
depends_on:
  - 05-01
files_modified:
  - packages/domain/src/purchase-request.ts
  - packages/domain/src/__tests__/purchase-request.test.ts
  - packages/application/src/ports/purchase-request-store.ts
  - packages/application/src/services/purchase-request-service.ts
  - packages/application/src/__tests__/purchase-request-service.test.ts
  - packages/infrastructure/src/purchase/in-memory-purchase-request-store.ts
  - packages/infrastructure/src/index.ts
  - apps/web/src/app/lottery/[lotteryCode]/page.tsx
autonomous: true
requirements: [PURC-03, PURC-04]
user_setup: []
must_haves:
  truths:
    - confirmation UI shows immutable snapshot before creation
    - request snapshot stores quote and payload exactly as confirmed
  artifacts:
    - packages/domain/src/purchase-request.ts
    - packages/application/src/services/purchase-request-service.ts
    - apps/web/src/app/lottery/[lotteryCode]/page.tsx
  key_links:
    - packages/application/src/ports/purchase-request-store.ts
---

<objective>
Add confirmation dialog flow and immutable request snapshot creation layer that persists draft data before queue/ledger orchestration.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-purchase-request-orchestration/05-CONTEXT.md
@.planning/phases/05-purchase-request-orchestration/05-01-PLAN.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define immutable purchase request snapshot domain model</name>
  <files>packages/domain/src/purchase-request.ts, packages/domain/src/__tests__/purchase-request.test.ts, packages/domain/src/index.ts</files>
  <read_first>packages/domain/src/request-state.ts, packages/domain/src/purchase-draft.ts</read_first>
  <action>Create domain types for request snapshot (`requestId`, `userId`, `lotteryCode`, `drawId`, `payload`, `costMinor`, `currency`, `createdAt`) and status journal event entries. Include constructor/helper that freezes snapshot payload shape and starts journal with `awaiting_confirmation` event. Add tests for immutable payload cloning and initial journal event correctness.</action>
  <verify>corepack pnpm --filter @lottery/domain test</verify>
  <acceptance_criteria>
    - domain exports immutable request snapshot factory
    - journal starts with a single `awaiting_confirmation` event
    - tests fail if payload mutates after snapshot creation
  </acceptance_criteria>
  <done>Domain now has immutable request snapshot contracts for persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Add purchase request store port and creation service</name>
  <files>packages/application/src/ports/purchase-request-store.ts, packages/application/src/services/purchase-request-service.ts, packages/application/src/__tests__/purchase-request-service.test.ts, packages/infrastructure/src/purchase/in-memory-purchase-request-store.ts, packages/infrastructure/src/index.ts, packages/application/src/index.ts</files>
  <read_first>packages/application/src/ports/ledger-store.ts, packages/application/src/services/purchase-draft-service.ts</read_first>
  <action>Introduce `PurchaseRequestStore` port with append/read methods and in-memory adapter. Implement `PurchaseRequestService.createAwaitingConfirmation` that accepts validated quote result from `05-01`, creates immutable snapshot and initial journal entry, and persists it idempotently by requestId. Keep status at `awaiting_confirmation`; queue + reserve integration is deferred to `05-03`.</action>
  <verify>corepack pnpm --filter @lottery/application test; corepack pnpm --filter @lottery/infrastructure typecheck</verify>
  <acceptance_criteria>
    - request creation service persists immutable snapshot with journal
    - duplicate requestId returns replay-safe existing record
    - application tests assert snapshot immutability and journal persistence
  </acceptance_criteria>
  <done>Application/infrastructure layers can persist confirmation-ready request snapshots.</done>
</task>

<task type="auto">
  <name>Task 3: Add web confirmation dialog contour before final request creation</name>
  <files>apps/web/src/app/lottery/[lotteryCode]/page.tsx</files>
  <read_first>apps/web/src/app/lottery/[lotteryCode]/page.tsx, apps/web/src/lib/access/entry-flow.ts</read_first>
  <action>Split draft submit into two steps: (1) quote preparation (from `05-01`) and (2) explicit confirmation action that calls request creation service. Render confirmation block with lottery code, draw id, validated payload summary, and final cost. Only on confirm create immutable request snapshot. Provide cancel button that returns to editable form without writing request data.</action>
  <verify>corepack pnpm --filter @lottery/web build</verify>
  <acceptance_criteria>
    - user sees confirmation section with quote details before creation
    - request is created only after explicit confirm action
    - cancel path returns to form without persisted request
  </acceptance_criteria>
  <done>UI now enforces explicit confirmation before request snapshot creation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm --filter @lottery/domain test` passes
- [ ] `corepack pnpm --filter @lottery/application test` passes
- [ ] `corepack pnpm --filter @lottery/infrastructure typecheck` passes
- [ ] `corepack pnpm --filter @lottery/web build` passes
</verification>

<output>
After completion, create `.planning/phases/05-purchase-request-orchestration/05-02-SUMMARY.md`
</output>
