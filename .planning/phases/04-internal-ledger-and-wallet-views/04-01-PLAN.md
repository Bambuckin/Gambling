---
phase: 04-internal-ledger-and-wallet-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/domain/src/ledger.ts
  - packages/domain/src/__tests__/
  - packages/application/src/ports/
  - packages/application/src/services/
  - packages/application/src/__tests__/
  - packages/infrastructure/src/ledger/
  - apps/web/src/lib/ledger/
  - apps/web/src/app/lottery/[lotteryCode]/page.tsx
autonomous: true
requirements: [BAL-05]
user_setup: []
---

<objective>
Implement wallet aggregate baseline and immutable ledger entry model so all later reserve/debit/release flows can build on append-only financial history.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-internal-ledger-and-wallet-views/04-CONTEXT.md
@.planning/REQUIREMENTS.md
@docs/modules/boundary-catalog.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend domain ledger contracts with invariant-safe aggregate helpers</name>
  <files>packages/domain/src/ledger.ts, packages/domain/src/__tests__/</files>
  <read_first>packages/domain/src/ledger.ts, packages/domain/src/request-state.ts</read_first>
  <action>Define normalized wallet movement helpers that derive `availableMinor`/`reservedMinor` from immutable entries and reject invalid transitions (negative amount, mixed currency per wallet stream, reserve/debit/release underflow). Add unit tests that verify aggregate math and invariant failures.</action>
  <verify>corepack pnpm --filter @lottery/domain test</verify>
  <acceptance_criteria>
    - ledger domain exposes deterministic aggregate function over ordered entries
    - invariant violations throw explicit ledger validation error
    - domain tests cover reserve, debit, release, credit, and underflow rejection
  </acceptance_criteria>
  <done>Domain-level immutable ledger model and aggregate behavior are validated.</done>
</task>

<task type="auto">
  <name>Task 2: Add ledger port + wallet ledger service with append-only entry recording</name>
  <files>packages/application/src/ports/, packages/application/src/services/, packages/application/src/__tests__/, packages/infrastructure/src/ledger/</files>
  <read_first>packages/application/src/services/access-service.ts, packages/application/src/ports/draw-store.ts, packages/infrastructure/src/draw/in-memory-draw-store.ts</read_first>
  <action>Create `LedgerStore` port, `WalletLedgerService`, and in-memory adapter implementing append-only write and read-by-user flow. Service should sanitize entry input, emit deterministic entry IDs, and expose wallet snapshot + movement history methods for runtime consumption.</action>
  <verify>corepack pnpm --filter @lottery/application test; corepack pnpm --filter @lottery/infrastructure typecheck</verify>
  <acceptance_criteria>
    - application package exports ledger port and service entrypoints
    - in-memory ledger adapter stores immutable cloned entries and returns sorted user history
    - application tests validate entry append and wallet aggregate read path
  </acceptance_criteria>
  <done>Application/infrastructure layers can serve immutable wallet history and aggregate snapshot.</done>
</task>

<task type="auto">
  <name>Task 3: Wire web runtime to wallet aggregate read path (no reserve logic yet)</name>
  <files>apps/web/src/lib/ledger/, apps/web/src/app/lottery/[lotteryCode]/page.tsx</files>
  <read_first>apps/web/src/lib/access/access-runtime.ts, apps/web/src/lib/registry/registry-runtime.ts, apps/web/src/app/lottery/[lotteryCode]/page.tsx</read_first>
  <action>Add ledger runtime composition for local seeds and replace hash-based preview balance on lottery page with wallet aggregate values from `WalletLedgerService` (`available`, `reserved`, movement count). Keep this read-only in 04-01; reserve/debit/release mutation commands are added in 04-02.</action>
  <verify>corepack pnpm --filter @lottery/web build</verify>
  <acceptance_criteria>
    - lottery page no longer uses hash-derived fake balance function
    - page displays available and reserved values from ledger-backed service
    - web build succeeds with new ledger runtime wiring
  </acceptance_criteria>
  <done>Ledger-backed wallet read path is visible in runtime without mutation flows.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm --filter @lottery/domain test` passes
- [ ] `corepack pnpm --filter @lottery/application test` passes
- [ ] `corepack pnpm --filter @lottery/infrastructure typecheck` passes
- [ ] `corepack pnpm --filter @lottery/web build` passes
- [ ] Lottery page wallet block is ledger-backed (not hash-generated)
</verification>

<output>
After completion, create `.planning/phases/04-internal-ledger-and-wallet-views/04-01-SUMMARY.md`
</output>
