---
phase: 04-internal-ledger-and-wallet-views
plan: 02
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - packages/domain/src/ledger.ts
  - packages/application/src/services/wallet-ledger-service.ts
  - packages/application/src/__tests__/
  - docs/modules/boundary-catalog.md
autonomous: true
requirements: [BAL-01, BAL-02, BAL-03]
user_setup: []
---

<objective>
Implement reserve/debit/release transaction rules and idempotency guards so request lifecycle can mutate wallet state safely.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-internal-ledger-and-wallet-views/04-CONTEXT.md
@.planning/phases/04-internal-ledger-and-wallet-views/04-01-SUMMARY.md
@packages/domain/src/request-state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reserve/debit/release command API with state-safe invariants</name>
  <files>packages/application/src/services/wallet-ledger-service.ts, packages/domain/src/ledger.ts</files>
  <read_first>packages/application/src/services/wallet-ledger-service.ts, packages/domain/src/ledger.ts, packages/domain/src/request-state.ts</read_first>
  <action>Add explicit command methods for reserve, debit, and release. Enforce non-negative underflow prevention, consistent currency, and valid reference linking (`requestId` required for reserve/debit/release in this phase).</action>
  <verify>corepack pnpm --filter @lottery/application test</verify>
  <acceptance_criteria>
    - service exposes dedicated reserve/debit/release methods
    - reserve reduces available and increases reserved
    - debit consumes reserved without restoring available
    - release returns reserved back to available
  </acceptance_criteria>
  <done>Reserve/debit/release lifecycle behavior is encoded in service API, not ad-hoc caller logic.</done>
</task>

<task type="auto">
  <name>Task 2: Add idempotency guard semantics per request operation</name>
  <files>packages/application/src/services/wallet-ledger-service.ts, packages/application/src/__tests__/</files>
  <read_first>packages/application/src/services/wallet-ledger-service.ts, packages/application/src/__tests__/wallet-ledger-service.test.ts</read_first>
  <action>Guarantee repeated command execution with same idempotency key returns previously written entry result instead of appending duplicates. Add tests covering retry of reserve, debit, and release with identical keys.</action>
  <verify>corepack pnpm --filter @lottery/application test</verify>
  <acceptance_criteria>
    - duplicate idempotency calls do not append additional entries
    - idempotent replays return stable aggregate outcome
    - tests cover repeated command calls across reserve/debit/release
  </acceptance_criteria>
  <done>Ledger command surface is safe for retries and queue replays.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh boundary docs for financial mutation ownership</name>
  <files>docs/modules/boundary-catalog.md</files>
  <read_first>docs/modules/boundary-catalog.md, packages/application/src/services/wallet-ledger-service.ts</read_first>
  <action>Document that financial mutation rules and idempotency live in application ledger service boundaries and must not be implemented in route files or adapters.</action>
  <verify>Select-String -Path docs/modules/boundary-catalog.md -Pattern "ledger service|idempotency|reserve/debit/release"</verify>
  <acceptance_criteria>
    - boundary catalog states where reserve/debit/release and idempotency are owned
  </acceptance_criteria>
  <done>Financial mutation ownership is explicit for future phases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm --filter @lottery/application test` passes
- [ ] reserve/debit/release commands enforce wallet invariants
- [ ] idempotency replay does not duplicate entries
</verification>

<output>
After completion, create `.planning/phases/04-internal-ledger-and-wallet-views/04-02-SUMMARY.md`
</output>
