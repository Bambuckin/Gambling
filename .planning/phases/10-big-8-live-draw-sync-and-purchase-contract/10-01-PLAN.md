---
phase: 10-big-8-live-draw-sync-and-purchase-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/domain/src/access.ts
  - packages/domain/src/lottery-registry.ts
  - packages/domain/src/purchase-draft.ts
  - packages/application/src/services/draw-refresh-service.ts
  - packages/application/src/services/purchase-draft-service.ts
  - packages/infrastructure/src/postgres/postgres-schema.ts
  - packages/infrastructure/src/postgres/postgres-access-store.ts
  - packages/infrastructure/src/access/in-memory-identity-store.ts
  - packages/infrastructure/src/seeds/default-lottery-catalog.ts
  - apps/web/src/lib/draw/draw-runtime.ts
  - apps/web/src/lib/lottery-form/render-lottery-form-fields.tsx
  - apps/web/src/app/lottery/[lotteryCode]/page.tsx
  - docs/modules/big8-terminal-integration.md
autonomous: true
requirements: []
user_setup:
  - install Node.js 20+ on the terminal machine
  - install a managed Chromium/Chrome browser on the terminal machine
  - prepare a dedicated logged-in National Lottery terminal session for automation bootstrap
must_haves:
  truths:
    - Big 8 draws come from the live terminal UI, not from seeded snapshots
    - one request can contain multiple Big 8 tickets and must preserve board selections exactly
    - terminal phone comes from the authenticated user account, not cashier free text
  artifacts:
    - packages/domain/src/purchase-draft.ts
    - apps/web/src/app/lottery/[lotteryCode]/page.tsx
    - docs/modules/big8-terminal-integration.md
  key_links:
    - apps/web/src/lib/draw/draw-runtime.ts
    - apps/terminal-worker/src/lib/terminal-handler-runtime.ts
---

<objective>
Implement the first live Big 8 purchase slice by introducing real draw ingestion, account-phone plumbing, and a multi-ticket Big 8 draft contract that the cashier UI can submit to the queue.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-big-8-live-draw-sync-and-purchase-contract/10-CONTEXT.md
@docs/modules/big8-terminal-integration.md
@apps/web/src/app/lottery/[lotteryCode]/page.tsx
@packages/domain/src/access.ts
@packages/domain/src/purchase-draft.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend user identity/profile data with normalized phone number</name>
  <files>packages/domain/src/access.ts, packages/infrastructure/src/postgres/postgres-schema.ts, packages/infrastructure/src/postgres/postgres-access-store.ts, packages/infrastructure/src/access/in-memory-identity-store.ts</files>
  <read_first>packages/domain/src/access.ts, packages/infrastructure/src/postgres/postgres-access-store.ts</read_first>
  <action>Add a normalized phone field to the access identity model and persist it in both in-memory and Postgres identity stores. Update schema bootstrap and row mappers so the web runtime can reliably read the terminal phone number from the authenticated user account. Do not add ad-hoc purchase-time phone input back into the route UI.</action>
  <verify>corepack pnpm --filter @lottery/application test -- access-service; corepack pnpm --filter @lottery/infrastructure typecheck</verify>
  <acceptance_criteria>
    - authenticated identity model exposes a normalized terminal phone number
    - Postgres schema/store persist and read the phone field
    - in-memory access path remains usable for local/dev mode
  </acceptance_criteria>
  <done>Account profile data can supply the terminal phone number to later purchase and automation flows.</done>
</task>

<task type="auto">
  <name>Task 2: Replace synthetic Big 8 draft payload with a structured multi-ticket contract</name>
  <files>packages/domain/src/lottery-registry.ts, packages/domain/src/purchase-draft.ts, packages/application/src/services/purchase-draft-service.ts, packages/infrastructure/src/seeds/default-lottery-catalog.ts</files>
  <read_first>packages/domain/src/lottery-registry.ts, packages/domain/src/purchase-draft.ts, packages/infrastructure/src/seeds/default-lottery-catalog.ts</read_first>
  <action>Model Big 8 as structured ticket boards instead of shallow `draw_count/multiplier` fields. Validation must enforce `8 of 20 + 1 of 4` per ticket, support multiple tickets in one draft, and keep deterministic pricing by ticket count and per-ticket multiplier rules. Update the seeded registry metadata so Big 8 no longer pretends to be a generic numeric form.</action>
  <verify>corepack pnpm --filter @lottery/domain test -- purchase-draft; corepack pnpm --filter @lottery/application test -- purchase-draft-service</verify>
  <acceptance_criteria>
    - Big 8 draft model supports repeated ticket boards in one request
    - validation rejects malformed boards and mismatched control numbers
    - deterministic quote output matches ticket-count-driven pricing
  </acceptance_criteria>
  <done>Domain and application layers understand the real Big 8 purchase shape instead of generic placeholder fields.</done>
</task>

<task type="auto">
  <name>Task 3: Add live Big 8 draw provider and shared refresh pipeline</name>
  <files>packages/application/src/services/draw-refresh-service.ts, apps/web/src/lib/draw/draw-runtime.ts, apps/terminal-worker/src/lib/terminal-handler-runtime.ts, docs/modules/big8-terminal-integration.md</files>
  <read_first>packages/application/src/services/draw-refresh-service.ts, apps/web/src/lib/draw/draw-runtime.ts, docs/modules/big8-terminal-integration.md</read_first>
  <action>Introduce a provider path that can fetch Big 8 draws from the live terminal UI instead of seeded snapshots. Use the documented React-props-first strategy with modal fallback. Persist fetched draws in shared storage with freshness metadata and make the runtime refresh cadence compatible with the 20-second target. Keep the automation read-only in this plan: draw scraping only, no cart mutation yet.</action>
  <verify>corepack pnpm --filter @lottery/application test -- draw-refresh-service; corepack pnpm --filter @lottery/terminal-worker typecheck</verify>
  <acceptance_criteria>
    - Big 8 draw provider can upsert live draw snapshots through DrawRefreshService
    - seeded snapshots are no longer the source of truth for Big 8 when live runtime is enabled
    - integration notes document provider assumptions and fallback behavior
  </acceptance_criteria>
  <done>Shared backend can serve live Big 8 draw choices to cashier clients.</done>
</task>

<task type="auto">
  <name>Task 4: Rebuild the Big 8 cashier page around live draws and multi-ticket drafting</name>
  <files>apps/web/src/lib/lottery-form/render-lottery-form-fields.tsx, apps/web/src/app/lottery/[lotteryCode]/page.tsx</files>
  <read_first>apps/web/src/app/lottery/[lotteryCode]/page.tsx, apps/web/src/lib/lottery-form/render-lottery-form-fields.tsx</read_first>
  <action>Replace the placeholder field renderer for Big 8 with a cashier-facing board editor that supports multiple tickets, live draw selection, nearest-draw default, and automatic account-phone usage. The server action must prepare immutable queue-ready snapshots using the new structured payload while preserving stale/missing draw blocking. UI should mirror the repeated-card mental model from the real site, not a generic key/value form.</action>
  <verify>corepack pnpm --filter @lottery/web build</verify>
  <acceptance_criteria>
    - cashier can add/remove multiple Big 8 ticket boards on one page
    - live draw selector defaults to nearest draw and allows explicit override
    - draft confirmation reflects selected draw, ticket count, and deterministic cost
  </acceptance_criteria>
  <done>Cashier client can prepare real Big 8 queue requests against live draw data.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm --filter @lottery/domain test -- purchase-draft` passes
- [ ] `corepack pnpm --filter @lottery/application test -- access-service draw-refresh-service purchase-draft-service` passes
- [ ] `corepack pnpm --filter @lottery/terminal-worker typecheck` passes
- [ ] `corepack pnpm --filter @lottery/web build` passes
- [ ] Big 8 cashier page uses live draw options and multiple structured ticket boards
</verification>

<output>
After completion, create `.planning/phases/10-big-8-live-draw-sync-and-purchase-contract/10-01-SUMMARY.md`
</output>
