---
phase: 01-foundation-contracts
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/web/package.json
  - apps/web/src/app/page.tsx
  - apps/web/src/app/layout.tsx
  - apps/terminal-worker/package.json
  - apps/terminal-worker/src/main.ts
  - packages/domain/package.json
  - packages/domain/src/index.ts
  - packages/application/package.json
  - packages/application/src/index.ts
  - packages/infrastructure/package.json
  - packages/infrastructure/src/index.ts
  - packages/lottery-handlers/package.json
  - packages/lottery-handlers/src/index.ts
  - packages/test-kit/package.json
  - packages/test-kit/src/index.ts
autonomous: true
requirements: [PLAT-01, PLAT-03]
user_setup: []
must_haves:
  truths:
    - "Repository contains concrete runtime apps and shared packages matching the selected Phase 1 architecture"
    - "The workspace can install and type-check the initial app/package scaffold"
    - "Later plans have clear locations to place domain, worker, handler, and test code"
  artifacts:
    - path: "apps/web/package.json"
      provides: "Web app package scaffold"
      contains: "\"name\""
    - path: "apps/terminal-worker/src/main.ts"
      provides: "Worker process entrypoint"
      min_lines: 10
    - path: "packages/domain/src/index.ts"
      provides: "Shared domain package export surface"
      min_lines: 5
    - path: "packages/test-kit/src/index.ts"
      provides: "Test-kit package export surface"
      min_lines: 5
  key_links:
    - from: "package.json"
      to: "apps/web/package.json"
      via: "workspace script targets web app"
    - from: "package.json"
      to: "apps/terminal-worker/package.json"
      via: "workspace script targets worker app"
---

<objective>
Create the concrete workspace skeleton defined by ADR-001 so later plans can add contracts and fake adapters into real locations.

Purpose: Move from abstract repo shape to actual directories, packages, and entrypoints.
Output: App/package scaffolds for web, worker, and shared modules.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-contracts/01-CONTEXT.md
@docs/adr/ADR-001-stack-and-repo-shape.md
@package.json
@pnpm-workspace.yaml
@tsconfig.base.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold runtime apps</name>
  <files>apps/web/package.json, apps/web/src/app/page.tsx, apps/web/src/app/layout.tsx, apps/terminal-worker/package.json, apps/terminal-worker/src/main.ts</files>
  <read_first>docs/adr/ADR-001-stack-and-repo-shape.md, package.json, pnpm-workspace.yaml</read_first>
  <action>Create `apps/web` and `apps/terminal-worker` according to ADR-001. The web app should have a minimal bootable entry route and layout that identify the project and Phase 1 scaffold status. The worker app should expose a `main.ts` entrypoint that can later host queue processing and schedulers. Keep both apps TypeScript-first and aligned with root scripts.</action>
  <verify>Test-Path apps/web/package.json; Test-Path apps/terminal-worker/src/main.ts</verify>
  <acceptance_criteria>
    - apps/web/package.json exists
    - apps/web/src/app/page.tsx exists
    - apps/terminal-worker/src/main.ts exists
  </acceptance_criteria>
  <done>Web and worker runtime roots exist and match ADR-001.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold shared packages</name>
  <files>packages/domain/package.json, packages/domain/src/index.ts, packages/application/package.json, packages/application/src/index.ts, packages/infrastructure/package.json, packages/infrastructure/src/index.ts, packages/lottery-handlers/package.json, packages/lottery-handlers/src/index.ts, packages/test-kit/package.json, packages/test-kit/src/index.ts</files>
  <read_first>docs/adr/ADR-001-stack-and-repo-shape.md</read_first>
  <action>Create shared package skeletons for domain, application, infrastructure, lottery handlers, and test kit. Each package should have a `package.json`, `src/index.ts`, and exports ready for later plans. Use names and responsibilities that mirror the module map from the phase context.</action>
  <verify>Test-Path packages/domain/src/index.ts; Test-Path packages/test-kit/src/index.ts</verify>
  <acceptance_criteria>
    - Each package directory exists with package.json and src/index.ts
    - Package names reflect their responsibility (`domain`, `application`, `infrastructure`, `lottery-handlers`, `test-kit`)
  </acceptance_criteria>
  <done>Shared packages exist as real workspace members, not placeholders in docs only.</done>
</task>

<task type="auto">
  <name>Task 3: Wire workspace scripts and TypeScript project references</name>
  <files>package.json, tsconfig.base.json, apps/web/package.json, apps/terminal-worker/package.json, packages/domain/package.json, packages/application/package.json, packages/infrastructure/package.json, packages/lottery-handlers/package.json, packages/test-kit/package.json</files>
  <read_first>package.json, tsconfig.base.json, apps/web/package.json, apps/terminal-worker/package.json, packages/domain/package.json</read_first>
  <action>Update root and package-level scripts so the scaffold can be installed and type-checked consistently. Add any needed TypeScript project references or shared config extensions so later packages can build without redefining the baseline. Keep scripts aligned with the root commands introduced in Plan 01.</action>
  <verify>Select-String -Path package.json -Pattern "dev:web|dev:worker|typecheck"; Select-String -Path apps/web/package.json -Pattern "typecheck"</verify>
  <acceptance_criteria>
    - Root package.json still exposes dev and typecheck scripts after package scaffolding
    - Runtime apps and shared packages contain scripts or config needed for workspace-wide type checking
  </acceptance_criteria>
  <done>Workspace members are wired into the root tooling baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- **TB-01:** Root workspace config → runtime apps and shared packages
- **TB-02:** Shared packages → future feature plans

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| 01-02-T1 | Tampering | Repo shape | mitigate | Create scaffold strictly from ADR-001 and verify expected directories/files |
| 01-02-T2 | Denial of service | Workspace tooling | mitigate | Keep all workspace members wired to root scripts and shared TS config |
| 01-02-T3 | Information disclosure | Mixed concerns between apps and packages | mitigate | Separate runtime entrypoints from shared business packages immediately |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] Runtime app directories exist
- [ ] Shared package directories exist
- [ ] Root scripts still point to real workspace members
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Future plans have real filesystem targets for domain, worker, handler, and test code
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-contracts/01-02-SUMMARY.md`
</output>
