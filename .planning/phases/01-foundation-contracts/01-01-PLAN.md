---
phase: 01-foundation-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/README.md
  - docs/adr/ADR-001-stack-and-repo-shape.md
  - package.json
  - pnpm-workspace.yaml
  - tsconfig.base.json
autonomous: true
requirements: [PLAT-01, DOCS-01]
user_setup: []
must_haves:
  truths:
    - "Repository has an explicit ADR selecting the Phase 1 stack and repo shape"
    - "Root workspace files exist and describe the chosen monorepo baseline"
    - "README and docs map tell a future session where to start and how the repo is organized"
  artifacts:
    - path: "docs/adr/ADR-001-stack-and-repo-shape.md"
      provides: "Chosen stack and repository shape"
      min_lines: 40
    - path: "package.json"
      provides: "Root workspace scripts and package manager metadata"
      contains: "\"typecheck\""
    - path: "pnpm-workspace.yaml"
      provides: "Workspace package boundaries"
      contains: "packages:"
    - path: "tsconfig.base.json"
      provides: "Shared TypeScript baseline"
      contains: "\"compilerOptions\""
  key_links:
    - from: "docs/adr/ADR-001-stack-and-repo-shape.md"
      to: "pnpm-workspace.yaml"
      via: "documented repo shape matches workspace config"
    - from: "README.md"
      to: "docs/README.md"
      via: "repo entrypoint points to standing docs"
---

<objective>
Select and codify the foundation stack and repository shape, then create the root workspace files that every later plan depends on.

Purpose: Later plans should not guess the repo structure or tooling.
Output: ADR-001, root workspace config, and updated repo entrypoint docs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-contracts/01-RESEARCH.md
@docs/adr/ADR-000-project-principles.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ADR-001 for stack and repo shape</name>
  <files>docs/adr/ADR-001-stack-and-repo-shape.md</files>
  <read_first>.planning/PROJECT.md, .planning/research/STACK.md, .planning/phases/01-foundation-contracts/01-CONTEXT.md, docs/adr/ADR-000-project-principles.md</read_first>
  <action>Create `docs/adr/ADR-001-stack-and-repo-shape.md` that compares at least two viable TypeScript-first options, then selects one baseline. The selected baseline must include: `pnpm` workspace monorepo, `apps/web` for the shared user/admin web surface, `apps/terminal-worker` for queue execution and schedulers, `packages/domain`, `packages/application`, `packages/infrastructure`, `packages/lottery-handlers`, and `packages/test-kit`. Document why the choice supports one-terminal execution, transactional balance handling, fake adapters, and future-session resumability.</action>
  <verify>Test-Path docs/adr/ADR-001-stack-and-repo-shape.md</verify>
  <acceptance_criteria>
    - docs/adr/ADR-001-stack-and-repo-shape.md exists
    - ADR-001 contains the exact strings "apps/web", "apps/terminal-worker", and "packages/test-kit"
    - ADR-001 includes a clear "Decision" section naming the selected baseline
  </acceptance_criteria>
  <done>ADR-001 selects one baseline and explains the choice against project constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize root workspace files</name>
  <files>package.json, pnpm-workspace.yaml, tsconfig.base.json</files>
  <read_first>docs/adr/ADR-001-stack-and-repo-shape.md</read_first>
  <action>Create a root `package.json` with scripts named `dev:web`, `dev:worker`, `lint`, `typecheck`, `test`, and `smoke`. Create `pnpm-workspace.yaml` covering `apps/*`, `packages/*`, and optionally `tooling/*` if needed. Create `tsconfig.base.json` with shared strict TypeScript defaults that later apps and packages can extend. Keep values aligned with ADR-001.</action>
  <verify>Test-Path package.json; Test-Path pnpm-workspace.yaml; Test-Path tsconfig.base.json</verify>
  <acceptance_criteria>
    - package.json contains "dev:web", "dev:worker", "typecheck", "test", and "smoke"
    - pnpm-workspace.yaml contains "apps/*" and "packages/*"
    - tsconfig.base.json contains "strict"
  </acceptance_criteria>
  <done>Root workspace files exist and reflect the selected monorepo baseline.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh root entrypoint docs for the chosen baseline</name>
  <files>README.md, docs/README.md</files>
  <read_first>README.md, docs/README.md, docs/adr/ADR-001-stack-and-repo-shape.md, package.json, pnpm-workspace.yaml</read_first>
  <action>Update `README.md` and `docs/README.md` so a future agent can immediately see the selected stack, the workspace layout, the Phase 1 plan files, and the exact next execution step. Mention the chosen app/package directories and the root scripts from `package.json`.</action>
  <verify>Select-String -Path README.md -Pattern "apps/web|apps/terminal-worker"; Select-String -Path docs/README.md -Pattern "ADR-001|workspace"</verify>
  <acceptance_criteria>
    - README.md references ADR-001 and the selected workspace layout
    - docs/README.md references ADR-001 and the docs/session-independence rule
  </acceptance_criteria>
  <done>Repo entrypoint docs align with ADR-001 and workspace files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- **TB-01:** Planning artifacts → scaffolded workspace files
- **TB-02:** ADR-selected architecture → future implementation sessions

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| 01-01-T1 | Tampering | ADR/workspace alignment | mitigate | Write ADR-001 first, then verify workspace files match it |
| 01-01-T2 | Repudiation | Future session bootstrap | mitigate | README and docs map must point to exact start files and scripts |
| 01-01-T3 | Denial of service | Tooling drift | mitigate | Keep root scripts explicit and shared TS config centralized |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `package.json`, `pnpm-workspace.yaml`, and `tsconfig.base.json` exist
- [ ] ADR-001 explicitly names the chosen baseline
- [ ] README and docs map reference ADR-001 and workspace layout
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 has a concrete technical baseline instead of an open-ended stack question
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-contracts/01-01-SUMMARY.md`
</output>
