---
phase: 01-foundation-contracts
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - packages/domain/src/request-state.ts
  - packages/domain/src/ledger.ts
  - packages/domain/src/lottery-registry.ts
  - packages/domain/src/draw.ts
  - packages/domain/src/ticket.ts
  - packages/domain/src/index.ts
  - packages/application/src/ports/terminal-executor.ts
  - packages/application/src/ports/queue.ts
  - packages/application/src/ports/time-source.ts
  - packages/application/src/index.ts
  - packages/lottery-handlers/src/contracts.ts
  - packages/test-kit/src/fake-terminal.ts
  - packages/test-kit/src/fake-lottery-handler.ts
  - packages/test-kit/src/index.ts
  - packages/domain/src/__tests__/request-state.test.ts
autonomous: true
requirements: [PLAT-02, PLAT-03]
user_setup: []
must_haves:
  truths:
    - "Core domain and application contracts exist before feature implementation begins"
    - "Terminal and lottery integrations can be replaced by fake adapters in local development"
    - "At least one automated check validates the request state machine without the production terminal"
  artifacts:
    - path: "packages/domain/src/request-state.ts"
      provides: "Request status model and transitions"
      contains: "created"
    - path: "packages/application/src/ports/terminal-executor.ts"
      provides: "Terminal execution port"
      exports: ["TerminalExecutor"]
    - path: "packages/test-kit/src/fake-terminal.ts"
      provides: "Fake terminal adapter"
      min_lines: 20
    - path: "packages/domain/src/__tests__/request-state.test.ts"
      provides: "State-machine verification"
      contains: "describe("
  key_links:
    - from: "packages/test-kit/src/fake-terminal.ts"
      to: "packages/application/src/ports/terminal-executor.ts"
      via: "fake adapter implements terminal port"
    - from: "packages/lottery-handlers/src/contracts.ts"
      to: "packages/domain/src/lottery-registry.ts"
      via: "handler contract aligns with registry binding model"
---

<objective>
Define the shared domain contracts and fake adapters that make the future system testable in slices instead of forcing immediate production integration.

Purpose: Give later phases stable types, ports, and stubs to build against.
Output: Core domain models, application ports, fake adapters, and state-machine verification.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-contracts/01-RESEARCH.md
@docs/adr/ADR-001-stack-and-repo-shape.md
@packages/domain/src/index.ts
@packages/application/src/index.ts
@packages/test-kit/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define core domain models and state transitions</name>
  <files>packages/domain/src/request-state.ts, packages/domain/src/ledger.ts, packages/domain/src/lottery-registry.ts, packages/domain/src/draw.ts, packages/domain/src/ticket.ts, packages/domain/src/index.ts</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/research/ARCHITECTURE.md, .planning/research/PITFALLS.md</read_first>
  <action>Create explicit domain models for purchase request lifecycle, ledger operation types, lottery registry entries, draw snapshots, and ticket results. In `request-state.ts`, encode valid transitions for created, awaiting confirmation, confirmed, queued, executing, retrying, success, canceled, error, and reserve released states. Re-export the public domain surface from `packages/domain/src/index.ts`.</action>
  <verify>Test-Path packages/domain/src/request-state.ts; Test-Path packages/domain/src/index.ts</verify>
  <acceptance_criteria>
    - request-state.ts contains all roadmap-required request states
    - domain index exports the new contract files
  </acceptance_criteria>
  <done>Domain package exposes the core models future features need.</done>
</task>

<task type="auto">
  <name>Task 2: Define application ports and fake adapters</name>
  <files>packages/application/src/ports/terminal-executor.ts, packages/application/src/ports/queue.ts, packages/application/src/ports/time-source.ts, packages/application/src/index.ts, packages/lottery-handlers/src/contracts.ts, packages/test-kit/src/fake-terminal.ts, packages/test-kit/src/fake-lottery-handler.ts, packages/test-kit/src/index.ts</files>
  <read_first>packages/domain/src/index.ts, .planning/phases/01-foundation-contracts/01-CONTEXT.md</read_first>
  <action>Create application-layer ports for terminal execution, queue ownership, and time access. Define lottery-handler contracts in `packages/lottery-handlers/src/contracts.ts` so registry bindings can reference stable handler interfaces. Implement fake terminal and fake lottery-handler adapters in `packages/test-kit` that satisfy the new ports and can later drive local smoke flows.</action>
  <verify>Test-Path packages/application/src/ports/terminal-executor.ts; Test-Path packages/test-kit/src/fake-terminal.ts</verify>
  <acceptance_criteria>
    - terminal-executor.ts exports a stable terminal port interface
    - contracts.ts defines a lottery handler contract
    - fake-terminal.ts and fake-lottery-handler.ts exist and align with those contracts
  </acceptance_criteria>
  <done>Application ports and fake adapters exist as stable interfaces rather than ad-hoc implementation details.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated verification for the state machine baseline</name>
  <files>packages/domain/src/__tests__/request-state.test.ts</files>
  <read_first>packages/domain/src/request-state.ts</read_first>
  <action>Create an automated test file that proves allowed request-state transitions and rejects invalid ones. Include cases for reserve-related failure paths and no-double-execution assumptions. Use the test runner selected in ADR-001.</action>
  <verify>Test-Path packages/domain/src/__tests__/request-state.test.ts</verify>
  <acceptance_criteria>
    - request-state.test.ts exists
    - tests cover at least one happy path and one invalid transition
  </acceptance_criteria>
  <done>The request-state baseline is executable and locally verifiable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- **TB-01:** Shared domain contracts → later purchase and balance implementations
- **TB-02:** Fake adapters → future local smoke flows

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| 01-03-T1 | Tampering | State transitions | mitigate | Encode explicit allowed transitions and verify them with tests |
| 01-03-T2 | Spoofing | Terminal integration boundary | mitigate | Require all terminal interactions through a typed port and fake implementation |
| 01-03-T3 | Repudiation | Lottery handler binding | mitigate | Define handler contracts before concrete handlers appear |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] Core domain contract files exist
- [ ] Application ports exist
- [ ] Fake adapters exist
- [ ] Request-state verification file exists
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Future phases can build against stable contracts instead of inventing them ad hoc
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-contracts/01-03-SUMMARY.md`
</output>
