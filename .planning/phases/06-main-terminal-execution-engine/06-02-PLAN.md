---
phase: 06-main-terminal-execution-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - packages/application/src/ports/terminal-handler-registry.ts
  - packages/application/src/services/terminal-handler-resolver-service.ts
  - packages/application/src/__tests__/terminal-handler-resolver-service.test.ts
  - packages/application/src/index.ts
  - packages/lottery-handlers/src/registry.ts
  - packages/lottery-handlers/src/index.ts
  - apps/terminal-worker/src/lib/terminal-handler-runtime.ts
  - docs/modules/boundary-catalog.md
autonomous: true
requirements: [TERM-03]
user_setup: []
must_haves:
  truths:
    - terminal execution resolves handler by lottery code from predefined registry bindings
    - no runtime code generation from request payload is used for terminal execution
  artifacts:
    - packages/application/src/services/terminal-handler-resolver-service.ts
    - packages/lottery-handlers/src/registry.ts
    - apps/terminal-worker/src/lib/terminal-handler-runtime.ts
  key_links:
    - packages/lottery-handlers/src/contracts.ts
---

<objective>
Add deterministic terminal handler registry/resolver boundary so execution uses predefined handlers selected by lottery code and binding key.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-main-terminal-execution-engine/06-CONTEXT.md
@packages/lottery-handlers/src/contracts.ts
@packages/application/src/ports/terminal-executor.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define handler registry port and resolver service contracts</name>
  <files>packages/application/src/ports/terminal-handler-registry.ts, packages/application/src/services/terminal-handler-resolver-service.ts, packages/application/src/__tests__/terminal-handler-resolver-service.test.ts, packages/application/src/index.ts</files>
  <read_first>packages/application/src/ports/terminal-executor.ts, packages/application/src/services/purchase-execution-queue-service.ts</read_first>
  <action>Create registry port exposing `getPurchaseHandler(lotteryCode)` and return deterministic binding metadata (contract version + binding key). Implement resolver service that validates handler existence and throws typed error for unsupported lottery code. Add tests for success path and missing binding rejection.</action>
  <verify>corepack pnpm --filter @lottery/application test -- terminal-handler-resolver-service</verify>
  <acceptance_criteria>
    - application exposes typed registry port for terminal purchase handlers
    - resolver service rejects unknown lottery codes with explicit error code
    - resolver tests cover deterministic mapping and failure path
  </acceptance_criteria>
  <done>Application layer owns deterministic handler resolution boundary for terminal execution.</done>
</task>

<task type="auto">
  <name>Task 2: Add lottery-handlers registry implementation and exports</name>
  <files>packages/lottery-handlers/src/registry.ts, packages/lottery-handlers/src/index.ts</files>
  <read_first>packages/lottery-handlers/src/contracts.ts, packages/test-kit/src/fake-lottery-handler.ts</read_first>
  <action>Implement registry helper `createLotteryPurchaseHandlerRegistry()` that accepts predefined handler list, enforces one binding per lottery code, and provides read API for resolver service. Export helper from package index.</action>
  <verify>corepack pnpm --filter @lottery/lottery-handlers typecheck</verify>
  <acceptance_criteria>
    - handler registry rejects duplicate lottery bindings
    - package index exports registry helper and related types
    - typecheck passes for lottery-handlers package
  </acceptance_criteria>
  <done>Handler registration is deterministic and centrally validated.</done>
</task>

<task type="auto">
  <name>Task 3: Wire worker runtime to resolver service and document boundary rule</name>
  <files>apps/terminal-worker/src/lib/terminal-handler-runtime.ts, apps/terminal-worker/src/main.ts, docs/modules/boundary-catalog.md</files>
  <read_first>apps/terminal-worker/src/main.ts, docs/modules/boundary-catalog.md</read_first>
  <action>Add worker runtime module that composes predefined handlers, injects resolver service, and resolves handler by queued request lottery code before execution. Update boundary catalog to state worker may call resolver only through application contracts and cannot build handlers from request payload text.</action>
  <verify>corepack pnpm --filter @lottery/terminal-worker typecheck; Select-String -Path docs/modules/boundary-catalog.md -Pattern "deterministic handler|payload"</verify>
  <acceptance_criteria>
    - worker imports handler runtime module and resolves handlers by lottery code
    - boundary catalog forbids runtime-generated terminal logic from user payload
    - terminal-worker typecheck passes
  </acceptance_criteria>
  <done>Worker execution path is bound to deterministic handler registry rules.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm --filter @lottery/application test -- terminal-handler-resolver-service` passes
- [ ] `corepack pnpm --filter @lottery/lottery-handlers typecheck` passes
- [ ] `corepack pnpm --filter @lottery/terminal-worker typecheck` passes
</verification>

<output>
After completion, create `.planning/phases/06-main-terminal-execution-engine/06-02-SUMMARY.md`
</output>
