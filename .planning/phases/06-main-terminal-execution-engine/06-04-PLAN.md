---
phase: 06-main-terminal-execution-engine
plan: 04
type: execute
wave: 3
depends_on:
  - 06-03
files_modified:
  - packages/domain/src/retry-policy.ts
  - packages/domain/src/__tests__/retry-policy.test.ts
  - packages/domain/src/index.ts
  - packages/application/src/services/terminal-retry-service.ts
  - packages/application/src/__tests__/terminal-retry-service.test.ts
  - packages/application/src/index.ts
  - apps/terminal-worker/src/main.ts
  - docs/modules/boundary-catalog.md
autonomous: true
requirements: [TERM-05]
user_setup: []
must_haves:
  truths:
    - retry decisions are deterministic and configurable by policy
    - exhausted retries move request to final error state with preserved attempt history
  artifacts:
    - packages/domain/src/retry-policy.ts
    - packages/application/src/services/terminal-retry-service.ts
    - packages/application/src/__tests__/terminal-retry-service.test.ts
  key_links:
    - apps/terminal-worker/src/main.ts
---

<objective>
Implement retry classification and exhausted-failure handling so transient terminal failures retry safely and hard failures end with final error state.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-main-terminal-execution-engine/06-CONTEXT.md
@.planning/phases/06-main-terminal-execution-engine/06-03-PLAN.md
@packages/application/src/services/terminal-execution-attempt-service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add domain retry policy with deterministic classification</name>
  <files>packages/domain/src/retry-policy.ts, packages/domain/src/__tests__/retry-policy.test.ts, packages/domain/src/index.ts</files>
  <read_first>packages/domain/src/request-state.ts, packages/domain/src/terminal-attempt.ts</read_first>
  <action>Create policy helper accepting max attempts and classifier (`transient|terminal`) and returning next action (`retry` or `final_error`) for each attempt. Add tests for below-limit transient failure, limit-exhausted failure, and non-retryable terminal failure.</action>
  <verify>corepack pnpm --filter @lottery/domain test -- retry-policy</verify>
  <acceptance_criteria>
    - retry policy helper exists with explicit max-attempt input
    - tests cover retry and final-error branches deterministically
    - domain index exports retry policy helper
  </acceptance_criteria>
  <done>Retry decision logic is explicit, testable, and independent from worker script.</done>
</task>

<task type="auto">
  <name>Task 2: Build terminal retry service that mutates request/queue state</name>
  <files>packages/application/src/services/terminal-retry-service.ts, packages/application/src/__tests__/terminal-retry-service.test.ts, packages/application/src/index.ts</files>
  <read_first>packages/application/src/services/terminal-execution-attempt-service.ts, packages/application/src/services/purchase-execution-queue-service.ts</read_first>
  <action>Implement service that consumes normalized attempt result + retry policy output and applies transitions: `executing->retrying->queued` for retry path, or `executing->error` for exhausted/terminal failure. Ensure queue item status resets to `queued` on retry and is removed on final error.</action>
  <verify>corepack pnpm --filter @lottery/application test -- terminal-retry-service</verify>
  <acceptance_criteria>
    - retry path increments attempt and requeues request with `status=queued`
    - exhausted path transitions request to `error` and removes queue item
    - tests assert both branches and idempotent replay behavior
  </acceptance_criteria>
  <done>Application can deterministically decide and apply retry vs final error transitions.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate retry service in worker loop and update boundary docs</name>
  <files>apps/terminal-worker/src/main.ts, docs/modules/boundary-catalog.md</files>
  <read_first>apps/terminal-worker/src/main.ts, docs/modules/boundary-catalog.md</read_first>
  <action>Update worker loop so each failed attempt routes through `TerminalRetryService` rather than inline branching. Add boundary catalog note that retry policy is owned by application/domain services and worker entrypoint only orchestrates calls.</action>
  <verify>corepack pnpm --filter @lottery/terminal-worker typecheck; Select-String -Path docs/modules/boundary-catalog.md -Pattern "retry policy|final error"</verify>
  <acceptance_criteria>
    - worker no longer hardcodes retry/final-error branching logic
    - boundary catalog states retry ownership in application/domain
    - terminal-worker typecheck passes
  </acceptance_criteria>
  <done>Retry handling is centralized and worker orchestration stays thin.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm --filter @lottery/domain test -- retry-policy` passes
- [ ] `corepack pnpm --filter @lottery/application test -- terminal-retry-service` passes
- [ ] `corepack pnpm --filter @lottery/terminal-worker typecheck` passes
</verification>

<output>
After completion, create `.planning/phases/06-main-terminal-execution-engine/06-04-SUMMARY.md`
</output>
