---
phase: 06-main-terminal-execution-engine
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - packages/domain/src/terminal-attempt.ts
  - packages/domain/src/__tests__/terminal-attempt.test.ts
  - packages/domain/src/index.ts
  - packages/application/src/services/terminal-execution-attempt-service.ts
  - packages/application/src/__tests__/terminal-execution-attempt-service.test.ts
  - packages/application/src/index.ts
  - packages/application/src/ports/purchase-request-store.ts
  - apps/terminal-worker/src/main.ts
  - docs/runbooks/queue-incident-triage.md
autonomous: true
requirements: [TERM-04]
user_setup: []
must_haves:
  truths:
    - every execution attempt stores start/end timestamps and raw terminal output
    - attempt outcome is normalized to success/retrying/error before request transition
  artifacts:
    - packages/application/src/services/terminal-execution-attempt-service.ts
    - packages/domain/src/terminal-attempt.ts
    - docs/runbooks/queue-incident-triage.md
  key_links:
    - packages/application/src/ports/terminal-executor.ts
---

<objective>
Persist and normalize terminal execution attempt results (`startedAt`, `finishedAt`, `rawOutput`, `outcome`, `attempt metadata`) for each request.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-main-terminal-execution-engine/06-CONTEXT.md
@packages/application/src/ports/terminal-executor.ts
@packages/domain/src/purchase-request.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add domain attempt normalization model</name>
  <files>packages/domain/src/terminal-attempt.ts, packages/domain/src/__tests__/terminal-attempt.test.ts, packages/domain/src/index.ts</files>
  <read_first>packages/domain/src/request-state.ts, packages/domain/src/purchase-request.ts</read_first>
  <action>Create domain helper that builds normalized attempt record from raw terminal result, validates allowed outcomes (`success|retrying|error`), and produces journal note payload with structured metadata. Add tests for accepted outcomes and invalid result rejection.</action>
  <verify>corepack pnpm --filter @lottery/domain test -- terminal-attempt</verify>
  <acceptance_criteria>
    - domain exports normalized attempt result builder with strict outcome validation
    - tests cover success/retrying/error normalization and invalid outcome throw
    - domain index re-exports attempt helper
  </acceptance_criteria>
  <done>Attempt normalization is deterministic and protected by domain tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement terminal execution attempt service in application layer</name>
  <files>packages/application/src/services/terminal-execution-attempt-service.ts, packages/application/src/__tests__/terminal-execution-attempt-service.test.ts, packages/application/src/index.ts, packages/application/src/ports/purchase-request-store.ts</files>
  <read_first>packages/application/src/services/purchase-execution-queue-service.ts, packages/application/src/services/purchase-request-query-service.ts</read_first>
  <action>Create service `recordAttemptResult` that loads executing request, appends transition according to normalized outcome, writes attempt metadata note in journal, and updates queue item attempt/status fields for next step. Extend request-store read contract only if needed for typed attempt metadata retrieval.</action>
  <verify>corepack pnpm --filter @lottery/application test -- terminal-execution-attempt-service</verify>
  <acceptance_criteria>
    - service persists attempt metadata on each result recording call
    - executing->success and executing->retrying transitions are both covered by tests
    - application index exports new attempt service
  </acceptance_criteria>
  <done>Execution results are journaled with raw output and normalized outcome at application boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Wire worker execution path to write attempt metadata and refresh triage runbook</name>
  <files>apps/terminal-worker/src/main.ts, docs/runbooks/queue-incident-triage.md</files>
  <read_first>apps/terminal-worker/src/main.ts, docs/runbooks/queue-incident-triage.md</read_first>
  <action>After resolver executes terminal adapter, call `TerminalExecutionAttemptService.recordAttemptResult(...)` with explicit `startedAt`, `finishedAt`, and `rawOutput`. Update queue incident runbook to include new attempt metadata fields and file locations for troubleshooting.</action>
  <verify>corepack pnpm --filter @lottery/terminal-worker typecheck; Select-String -Path docs/runbooks/queue-incident-triage.md -Pattern "startedAt|finishedAt|rawOutput|attempt"</verify>
  <acceptance_criteria>
    - worker path records terminal attempt metadata via application service
    - runbook lists attempt metadata fields for incident capture
    - terminal-worker typecheck passes
  </acceptance_criteria>
  <done>Attempt journaling is connected to worker runtime and operational triage docs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm --filter @lottery/domain test -- terminal-attempt` passes
- [ ] `corepack pnpm --filter @lottery/application test -- terminal-execution-attempt-service` passes
- [ ] `corepack pnpm --filter @lottery/terminal-worker typecheck` passes
</verification>

<output>
After completion, create `.planning/phases/06-main-terminal-execution-engine/06-03-SUMMARY.md`
</output>
