---
phase: 06-main-terminal-execution-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/domain/src/terminal-execution.ts
  - packages/domain/src/__tests__/terminal-execution.test.ts
  - packages/domain/src/index.ts
  - packages/application/src/ports/terminal-execution-lock.ts
  - packages/application/src/services/purchase-execution-queue-service.ts
  - packages/application/src/__tests__/purchase-execution-queue-service.test.ts
  - packages/application/src/index.ts
  - packages/infrastructure/src/purchase/in-memory-terminal-execution-lock.ts
  - packages/infrastructure/src/index.ts
  - apps/terminal-worker/src/main.ts
  - docs/modules/boundary-catalog.md
autonomous: true
requirements: [TERM-01, TERM-02]
user_setup: []
must_haves:
  truths:
    - only one request can be moved to executing at any moment
    - admin-priority queue items are reserved before regular queued items
  artifacts:
    - packages/application/src/services/purchase-execution-queue-service.ts
    - packages/application/src/ports/terminal-execution-lock.ts
    - packages/infrastructure/src/purchase/in-memory-terminal-execution-lock.ts
  key_links:
    - apps/terminal-worker/src/main.ts
---

<objective>
Implement queue-reservation worker service with exclusive terminal lock so only one request is actively executed and dequeue ordering is deterministic.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-main-terminal-execution-engine/06-CONTEXT.md
@packages/application/src/services/purchase-orchestration-service.ts
@packages/application/src/ports/purchase-queue-store.ts
@packages/domain/src/request-state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add domain queue-order helper for deterministic next-item selection</name>
  <files>packages/domain/src/terminal-execution.ts, packages/domain/src/__tests__/terminal-execution.test.ts, packages/domain/src/index.ts</files>
  <read_first>packages/domain/src/request-state.ts, packages/domain/src/purchase-request.ts</read_first>
  <action>Create `rankQueueForExecution(items)` in domain that sorts `admin-priority` first, then by `enqueuedAt`, then `requestId`. Export queue item contract used by application. Add tests for mixed-priority ordering and tie-break stability.</action>
  <verify>corepack pnpm --filter @lottery/domain test -- terminal-execution</verify>
  <acceptance_criteria>
    - `packages/domain/src/terminal-execution.ts` exports deterministic rank helper
    - tests include admin-priority-first and timestamp/requestId tie-break checks
    - `packages/domain/src/index.ts` re-exports terminal execution helpers
  </acceptance_criteria>
  <done>Queue ordering rules are isolated in domain and reusable across worker services.</done>
</task>

<task type="auto">
  <name>Task 2: Implement exclusive execution lock and queue reservation service</name>
  <files>packages/application/src/ports/terminal-execution-lock.ts, packages/application/src/services/purchase-execution-queue-service.ts, packages/application/src/__tests__/purchase-execution-queue-service.test.ts, packages/application/src/index.ts, packages/infrastructure/src/purchase/in-memory-terminal-execution-lock.ts, packages/infrastructure/src/index.ts</files>
  <read_first>packages/application/src/ports/purchase-queue-store.ts, packages/application/src/ports/purchase-request-store.ts, packages/application/src/services/purchase-request-query-service.ts</read_first>
  <action>Add `TerminalExecutionLock` port with `acquire(ownerId)` and `release(ownerId)`. Implement `PurchaseExecutionQueueService.reserveNextQueuedRequest(workerId)` that acquires lock, resolves next queue item through domain ranking, transitions request `queued|retrying -> executing`, increments queue attempt count, marks queue item status `executing`, and saves request+queue atomically in service flow. Return replay-safe null when lock is already held or queue is empty.</action>
  <verify>corepack pnpm --filter @lottery/application test -- purchase-execution-queue-service</verify>
  <acceptance_criteria>
    - lock port and in-memory adapter exist and enforce single owner
    - reservation service transitions selected request to `executing`
    - service tests cover lock contention and admin-priority dequeue order
  </acceptance_criteria>
  <done>Application can reserve exactly one next request for execution under explicit lock ownership.</done>
</task>

<task type="auto">
  <name>Task 3: Wire worker loop to reservation service and update boundary catalog</name>
  <files>apps/terminal-worker/src/main.ts, docs/modules/boundary-catalog.md</files>
  <read_first>apps/terminal-worker/src/main.ts, docs/modules/boundary-catalog.md</read_first>
  <action>Replace scaffold boot-only logs with polling loop that calls `PurchaseExecutionQueueService.reserveNextQueuedRequest("terminal-worker")`, logs reserved request id/attempt, and always releases lock on loop exit branch. Update boundary catalog entries so worker ownership includes queue reservation + execution lock, while retry/classification remains future plan scope.</action>
  <verify>corepack pnpm --filter @lottery/terminal-worker typecheck; Select-String -Path docs/modules/boundary-catalog.md -Pattern "execution lock|queue reservation"</verify>
  <acceptance_criteria>
    - worker entrypoint calls reservation service instead of only scaffold logs
    - boundary catalog explicitly mentions execution lock ownership in worker flow
    - worker typecheck succeeds after wiring
  </acceptance_criteria>
  <done>Terminal worker hosts real queue reservation loop with explicit lock boundary and docs alignment.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm --filter @lottery/domain test -- terminal-execution` passes
- [ ] `corepack pnpm --filter @lottery/application test -- purchase-execution-queue-service` passes
- [ ] `corepack pnpm --filter @lottery/terminal-worker typecheck` passes
</verification>

<output>
After completion, create `.planning/phases/06-main-terminal-execution-engine/06-01-SUMMARY.md`
</output>
