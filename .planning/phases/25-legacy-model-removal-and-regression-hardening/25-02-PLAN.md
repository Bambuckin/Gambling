---
phase: 25-legacy-model-removal-and-regression-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - packages/application/src/services/purchase-completion-service.ts
  - packages/application/src/services/ticket-persistence-service.ts
  - packages/infrastructure/src/postgres/postgres-purchase-store.ts
  - packages/infrastructure/src/postgres/postgres-schema.ts
  - docs/handoff-runtime.md
  - .planning/phases/25-legacy-model-removal-and-regression-hardening/25-02-SUMMARY.md
autonomous: true
requirements:
  - CONS-10
must_haves:
  truths:
    - active purchase, draw, and fulfillment runtime no longer depends on legacy write-model storage as an operational truth source
    - any remaining compatibility tables or fields are either removed or explicitly proven to be non-semantic leftovers
    - parity verification demonstrates purchase -> worker -> draw -> result -> fulfillment works without the legacy mirrors
  artifacts:
    - packages/infrastructure/src/postgres/postgres-purchase-store.ts
    - packages/infrastructure/src/postgres/postgres-schema.ts
    - packages/application/src/services/purchase-completion-service.ts
    - packages/application/src/services/ticket-persistence-service.ts
  key_links:
    - .planning/phases/25-legacy-model-removal-and-regression-hardening/25-01-SUMMARY.md
    - .planning/phases/24-advisory-lock-and-queue-transport-hardening/24-01-SUMMARY.md
    - docs/handoff-runtime.md
---

<objective>
Remove the remaining legacy write-model dependency only after parity validation proves the final Big 8 contour and winning flow stay stable without compatibility mirrors.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Audit and remove active legacy write-model dependencies</name>
  <files>packages/application/src/services/purchase-completion-service.ts, packages/application/src/services/ticket-persistence-service.ts, packages/infrastructure/src/postgres/postgres-purchase-store.ts, packages/infrastructure/src/postgres/postgres-schema.ts</files>
  <action>Identify which legacy request/ticket/verification/lock writes are still operationally required versus merely historical compatibility, then remove or demote only the ones proven safe to retire.</action>
  <acceptance_criteria>
    - active Big 8 purchase, draw-close, and winnings flows no longer depend on legacy write-model semantics
    - compatibility artifacts that remain are non-semantic and clearly documented
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Prove parity and sync the final migration docs</name>
  <files>docs/handoff-runtime.md, .planning/phases/25-legacy-model-removal-and-regression-hardening/25-02-SUMMARY.md</files>
  <action>Run parity-focused validation for the final contour, then update the runtime handoff so it describes the post-legacy truth model honestly.</action>
  <acceptance_criteria>
    - validation proves purchase -> worker -> draw -> result -> fulfillment still works after legacy removal
    - runtime docs stop describing legacy write models as current compatibility requirements
  </acceptance_criteria>
</task>

</tasks>
