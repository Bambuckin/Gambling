---
phase: 29
plan: 29-01
title: Admin one-step draw closure
status: completed
wave: 1
depends_on: [28-01]
files_modified:
  - apps/web/src/app/admin/page.tsx
  - apps/web/src/lib/purchase/admin-draw-monitor.tsx
  - packages/application/src/services/draw-closure-service.ts
  - packages/application/src/__tests__/draw-closure-service.test.ts
---

<objective>
Collapse admin draw close and result publication into one operator action, keep purchased-ticket pre-close state truthful, and stabilize manual draw creation layout.
</objective>

<tasks>
  <task id="1" name="Collapse admin draw UI to one close action">
    <read_first>
      - apps/web/src/app/admin/page.tsx
      - apps/web/src/lib/purchase/admin-draw-monitor.tsx
      - apps/web/src/lib/purchase/admin-status-presenter.ts
    </read_first>
    <action>
      Remove `onSettleDraw` from `AdminDrawMonitorProps`, remove `adminSettleDrawAction` and its prop wiring from `admin/page.tsx`, remove `handleSettle`, remove the separate closed-draw publication button, and change the close confirmation/success copy to state that the close action publishes results immediately. Replace the manual draw creation flex layout with a grid layout using stable column widths so the datetime input and submit button cannot overlap.
    </action>
    <acceptance_criteria>
      - `apps/web/src/lib/purchase/admin-draw-monitor.tsx` contains no `onSettleDraw`, `handleSettle`, `closedDraws`, or standalone `Опубликовать результат` button.
      - `apps/web/src/app/admin/page.tsx` contains no `adminSettleDrawAction` and no `.settleDraw(` call.
      - Manual draw creation form uses `display: "grid"` and `gridTemplateColumns`.
    </acceptance_criteria>
  </task>

  <task id="2" name="Remove dead settlement service path">
    <read_first>
      - packages/application/src/services/draw-closure-service.ts
      - packages/application/src/__tests__/draw-closure-service.test.ts
      - packages/domain/src/draw.ts
    </read_first>
    <action>
      Make `closeDraw()` settle and publish both `open` draws and legacy `closed` canonical draws. Remove `SettleDrawInput`, `SettleDrawResult`, and `settleDraw()` from `DrawClosureService` so no separate operator settlement path remains. Keep the existing close path publishing canonical result visibility, compatibility tickets, and notifications.
    </action>
    <acceptance_criteria>
      - `draw-closure-service.ts` contains no `settleDraw(`, `SettleDrawInput`, `SettleDrawResult`, or `alreadySettled`.
      - `closeDraw()` returns a `settled` canonical draw with `resultVisibility: "visible"` after a marked purchase.
      - Legacy canonical draws with `status: "closed"` can be completed through `closeDraw()`.
    </acceptance_criteria>
  </task>

  <task id="3" name="Validate one-step propagation">
    <read_first>
      - packages/application/src/__tests__/draw-closure-service.test.ts
      - packages/application/src/__tests__/ticket-query-service.test.ts
      - apps/web/src/lib/purchase/__tests__/admin-status-presenter.test.ts
    </read_first>
    <action>
      Update focused tests to protect close-time publication and purchased/pre-publication ticket labels, then run targeted application and web checks plus web typecheck.
    </action>
    <acceptance_criteria>
      - `corepack pnpm --filter @lottery/application test -- src/__tests__/draw-closure-service.test.ts src/__tests__/ticket-query-service.test.ts` passes.
      - `corepack pnpm --filter @lottery/web test -- src/lib/purchase/__tests__/admin-status-presenter.test.ts` passes.
      - `corepack pnpm --filter @lottery/web typecheck` passes.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- Targeted application tests for draw closure and ticket query.
- Targeted web presenter test.
- Web typecheck.
- String scan for removed separate publication UI and service calls.
</verification>

<must_haves>
- No admin-visible second publication action remains.
- Closing an active draw publishes result visibility immediately.
- A purchased pending ticket is labeled as purchased before draw closure.
- Manual draw creation layout is stable and does not depend on overlapping flex wrap behavior.
</must_haves>
