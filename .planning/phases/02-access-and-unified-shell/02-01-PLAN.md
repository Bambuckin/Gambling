---
phase: 02-access-and-unified-shell
plan: 01
type: execute
wave: 1
depends_on: ["01-04"]
files_modified:
  - packages/domain/src/access.ts
  - packages/domain/src/index.ts
  - packages/application/src/ports/identity-store.ts
  - packages/application/src/ports/session-store.ts
  - packages/application/src/ports/password-verifier.ts
  - packages/application/src/services/access-service.ts
  - packages/application/src/__tests__/access-service.test.ts
  - packages/application/src/index.ts
  - packages/application/package.json
  - packages/infrastructure/src/access/in-memory-identity-store.ts
  - packages/infrastructure/src/access/in-memory-session-store.ts
  - packages/infrastructure/src/access/sha256-password-verifier.ts
  - packages/infrastructure/src/index.ts
  - docs/modules/boundary-catalog.md
  - .planning/codebase/STRUCTURE.md
autonomous: true
requirements: [AUTH-03, AUTH-04]
user_setup: []
must_haves:
  truths:
    - "Access and session lifecycle contracts are defined outside runtime apps"
    - "Identity and session storage are port-driven and replaceable"
    - "Session validity rules are executable and covered by automated checks"
  artifacts:
    - path: "packages/domain/src/access.ts"
      provides: "Access identity/session domain contracts and lifecycle helpers"
      contains: "SESSION_ROLES"
    - path: "packages/application/src/services/access-service.ts"
      provides: "Use-case service for login/authenticate/logout lifecycle"
      contains: "class AccessService"
    - path: "packages/infrastructure/src/access/in-memory-session-store.ts"
      provides: "In-memory session repository adapter"
      contains: "class InMemorySessionStore"
    - path: "packages/application/src/__tests__/access-service.test.ts"
      provides: "Lifecycle tests for login/session/logout/expiry"
      contains: "describe("
  key_links:
    - from: "packages/application/src/services/access-service.ts"
      to: "packages/domain/src/access.ts"
      via: "service enforces domain lifecycle checks"
    - from: "packages/infrastructure/src/access/in-memory-identity-store.ts"
      to: "packages/application/src/ports/identity-store.ts"
      via: "adapter implements identity storage port"
---

<objective>
Implement the first executable access slice: domain contracts, storage ports, and session lifecycle orchestration.

Purpose: Phase 2 needs a hard auth/session core before shell routes and role guards.
Output: typed access/session model, in-memory adapters, and lifecycle tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-access-and-unified-shell/02-CONTEXT.md
@docs/modules/boundary-catalog.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define access domain contracts and lifecycle rules</name>
  <files>packages/domain/src/access.ts, packages/domain/src/index.ts</files>
  <read_first>packages/domain/src/request-state.ts, packages/domain/src/ledger.ts, .planning/REQUIREMENTS.md</read_first>
  <action>Create `packages/domain/src/access.ts` with role, identity, and session contracts plus lifecycle helpers for session status evaluation (`active`, `expired`, `revoked`) and validity checks by timestamp. Include optional `returnToLotteryCode` in session shape for post-login return flow. Export all new access contracts from `packages/domain/src/index.ts`.</action>
  <verify>Test-Path packages/domain/src/access.ts; Select-String -Path packages/domain/src/access.ts -Pattern "SESSION_ROLES|resolveSessionStatus|isSessionValid"</verify>
  <acceptance_criteria>
    - `packages/domain/src/access.ts` exists and contains session lifecycle helpers
    - `packages/domain/src/index.ts` exports `./access.js`
  </acceptance_criteria>
  <done>Domain now contains auth/session lifecycle truth independent of UI/runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Add access application ports and use-case service</name>
  <files>packages/application/src/ports/identity-store.ts, packages/application/src/ports/session-store.ts, packages/application/src/ports/password-verifier.ts, packages/application/src/services/access-service.ts, packages/application/src/index.ts</files>
  <read_first>packages/application/src/ports/time-source.ts, packages/application/src/ports/queue.ts, packages/domain/src/access.ts</read_first>
  <action>Add typed ports for identity lookup, session persistence, and password verification. Implement `AccessService` with `login`, `authenticate`, and `logout` methods that enforce active identity checks, password verification, session creation with expiry, revoked-session blocking, and explicit login failure reasons (`identity_not_found`, `identity_disabled`, `invalid_password`). Export new ports and service from `packages/application/src/index.ts`.</action>
  <verify>Test-Path packages/application/src/services/access-service.ts; Select-String -Path packages/application/src/services/access-service.ts -Pattern "login\\(|authenticate\\(|logout\\("</verify>
  <acceptance_criteria>
    - access ports exist under `packages/application/src/ports/`
    - `AccessService` exposes login/authenticate/logout lifecycle methods
    - login failure reasons include `identity_not_found`, `identity_disabled`, and `invalid_password`
  </acceptance_criteria>
  <done>Application layer can orchestrate auth/session flow without concrete storage coupling.</done>
</task>

<task type="auto">
  <name>Task 3: Implement in-memory access adapters and lifecycle tests</name>
  <files>packages/infrastructure/src/access/in-memory-identity-store.ts, packages/infrastructure/src/access/in-memory-session-store.ts, packages/infrastructure/src/access/sha256-password-verifier.ts, packages/infrastructure/src/index.ts, packages/application/src/__tests__/access-service.test.ts, packages/application/package.json</files>
  <read_first>packages/infrastructure/src/index.ts, packages/test-kit/src/fake-terminal.ts, packages/application/package.json, packages/domain/src/__tests__/request-state.test.ts</read_first>
  <action>Create in-memory identity/session repositories implementing the new application ports and add a SHA-256 password verifier adapter for deterministic local checks. Add application tests covering successful login, invalid-password login, session expiry rejection, and logout revocation behavior. Update `packages/application/package.json` test script to run the new test file via Vitest.</action>
  <verify>corepack pnpm --filter @lottery/application test</verify>
  <acceptance_criteria>
    - in-memory identity/session adapter classes compile and export from infrastructure index
    - `packages/application/src/__tests__/access-service.test.ts` exists with lifecycle scenarios
    - `corepack pnpm --filter @lottery/application test` passes
  </acceptance_criteria>
  <done>Access lifecycle works end-to-end in local memory adapters and is validated by tests.</done>
</task>

<task type="auto">
  <name>Task 4: Refresh module structure docs for the new access slice</name>
  <files>docs/modules/boundary-catalog.md, .planning/codebase/STRUCTURE.md</files>
  <read_first>docs/modules/boundary-catalog.md, .planning/codebase/STRUCTURE.md, packages/domain/src/access.ts, packages/application/src/services/access-service.ts</read_first>
  <action>Update boundary and structure docs to include access/session contracts, ports, and in-memory adapter locations so Phase 2 continuation can follow module ownership without rediscovery.</action>
  <verify>Select-String -Path docs/modules/boundary-catalog.md -Pattern "access|session"; Select-String -Path .planning/codebase/STRUCTURE.md -Pattern "access-service|in-memory-session-store"</verify>
  <acceptance_criteria>
    - boundary catalog references new access ownership in domain/application/infrastructure
    - structure map references new access files and folders
  </acceptance_criteria>
  <done>Docs match implemented access slice and remain session-independent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- **TB-01:** Access service input (login/password/session id) -> identity/session stores
- **TB-02:** Domain lifecycle checks -> runtime authorization decisions in later plans

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| 02-01-T1 | Spoofing | Login/session validation | mitigate | Require identity existence + password verification + non-revoked non-expired session checks |
| 02-01-T2 | Tampering | Session lifecycle state | mitigate | Centralize status resolution in domain helpers and enforce through service |
| 02-01-T3 | Repudiation | Access flow outcomes | mitigate | Return explicit failure reasons and deterministic session metadata for later audit wiring |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm --filter @lottery/application test` passes
- [ ] `corepack pnpm typecheck` passes
- [ ] Access contracts/ports/adapters are exported from package indices
- [ ] Boundary and structure docs mention the new access slice
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `02-02` can consume a ready access/session core instead of inventing auth logic in UI routes
</success_criteria>

<output>
After completion, create `.planning/phases/02-access-and-unified-shell/02-01-SUMMARY.md`
</output>
