---
phase: 02-access-and-unified-shell
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - packages/domain/src/access-audit.ts
  - packages/application/src/ports/access-audit-log.ts
  - packages/application/src/services/access-service.ts
  - packages/application/src/__tests__/access-service.test.ts
  - docs/runbooks/fake-terminal-smoke.md
autonomous: true
requirements: [AUTH-05]
user_setup: []
must_haves:
  truths:
    - "Login, logout, and denied access events are persisted through an audit port"
    - "Access lifecycle verification includes audit assertions"
  artifacts:
    - path: "packages/application/src/ports/access-audit-log.ts"
      provides: "Access audit logging boundary"
      contains: "append"
    - path: "packages/domain/src/access-audit.ts"
      provides: "Typed audit event shape for access events"
      contains: "AccessAuditEvent"
  key_links:
    - from: "packages/application/src/services/access-service.ts"
      to: "packages/application/src/ports/access-audit-log.ts"
      via: "access service emits audit events through port"
---

<objective>
Add access event logs and verification scenarios for authentication flows.

Purpose: Phase 2 requires traceable access actions before moving to Phase 3.
Output: audit event contracts, audit port integration, and verification coverage.
</objective>

<context>

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-access-and-unified-shell/02-CONTEXT.md

</context>

<tasks>

<task type="code">
  <name>Add access audit contracts and emit events from access lifecycle</name>
  <files>packages/domain/src/access-audit.ts, packages/application/src/ports/access-audit-log.ts, packages/application/src/services/access-service.ts</files>
  <action>Define typed access audit events (`login_success`, `login_denied`, `logout`) and wire `AccessService` to emit them via an audit port with actor/session metadata and timestamp.</action>
  <verify>corepack pnpm --filter @lottery/application test</verify>
  <done>Access lifecycle emits deterministic audit events through a dedicated boundary.</done>
</task>

</tasks>

<verification>
- `corepack pnpm --filter @lottery/application test`
- scenario check for login/logout/denied event emission
</verification>

<success_criteria>

- [ ] Login, logout, and denial events include actor + timestamp
- [ ] Access tests assert audit events alongside lifecycle outcomes
- [ ] Access runbook/docs mention verification path for auth events
</success_criteria>
