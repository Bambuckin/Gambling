---
phase: 02-access-and-unified-shell
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/page.tsx
  - apps/web/src/app/login/page.tsx
  - apps/web/src/app/lottery/[lotteryCode]/page.tsx
  - apps/web/src/lib/access/session-cookie.ts
  - apps/web/src/lib/access/entry-flow.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-04]
user_setup: []
must_haves:
  truths:
    - "Unauthenticated lottery entry is redirected to login"
    - "Successful login returns the user to the originally selected lottery route"
    - "Session cookie survives navigation and refresh"
  artifacts:
    - path: "apps/web/src/app/login/page.tsx"
      provides: "Login entrypoint for unauthenticated users"
      contains: "Login"
    - path: "apps/web/src/lib/access/session-cookie.ts"
      provides: "Session cookie read/write behavior"
      contains: "Set-Cookie"
  key_links:
    - from: "apps/web/src/app/lottery/[lotteryCode]/page.tsx"
      to: "apps/web/src/lib/access/entry-flow.ts"
      via: "lottery entry checks session and redirects to login when needed"
---

<objective>
Build the first unified shell routing layer for authenticated lottery entry.

Purpose: Auth/session core from `02-01` must be reachable from web routes.
Output: login route, protected lottery route, and return-to-lottery redirect flow.
</objective>

<context>

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-access-and-unified-shell/02-CONTEXT.md
@.planning/phases/02-access-and-unified-shell/02-01-SUMMARY.md

</context>

<tasks>

<task type="code">
  <name>Implement login and protected lottery shell routes</name>
  <files>apps/web/src/app/login/page.tsx, apps/web/src/app/lottery/[lotteryCode]/page.tsx, apps/web/src/lib/access/entry-flow.ts</files>
  <action>Create login page and protected lottery page flow: when session is missing/invalid, store target lottery code and redirect to `/login`; after successful login, return to `/lottery/[lotteryCode]` from stored target.</action>
  <verify>corepack pnpm --filter @lottery/web typecheck</verify>
  <done>Login + lottery routes are wired through a single shell entry flow with redirect-back behavior.</done>
</task>

</tasks>

<verification>
- `corepack pnpm --filter @lottery/web typecheck`
- route-level smoke via local web runtime
</verification>

<success_criteria>

- [ ] Unauthenticated lottery access redirects to login
- [ ] Post-login user returns to the selected lottery
- [ ] Session survives normal refresh/navigation
</success_criteria>
