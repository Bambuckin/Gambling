---
phase: 02-access-and-unified-shell
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - apps/web/src/lib/access/role-guard.ts
  - apps/web/src/app/admin/page.tsx
  - apps/web/src/app/lottery/[lotteryCode]/page.tsx
  - apps/web/src/middleware.ts
autonomous: true
requirements: [AUTH-03]
user_setup: []
must_haves:
  truths:
    - "Role checks exist for both user and admin routes"
    - "Unauthorized attempts are denied before action execution"
  artifacts:
    - path: "apps/web/src/lib/access/role-guard.ts"
      provides: "Reusable role guard utilities"
      contains: "requireRole"
  key_links:
    - from: "apps/web/src/middleware.ts"
      to: "apps/web/src/lib/access/role-guard.ts"
      via: "request boundary enforces role permissions"
---

<objective>
Enforce role boundaries in UI and request edges.

Purpose: Access flow is incomplete until user/admin permissions are blocked at boundaries.
Output: shared role guard utilities and protected route behavior.
</objective>

<context>

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-access-and-unified-shell/02-CONTEXT.md

</context>

<tasks>

<task type="code">
  <name>Implement role guards for server and route boundaries</name>
  <files>apps/web/src/lib/access/role-guard.ts, apps/web/src/middleware.ts, apps/web/src/app/admin/page.tsx</files>
  <action>Add explicit role checks for `user` and `admin` flows, block unauthorized access with deterministic deny handling, and apply guards before protected page rendering or server-side action entry.</action>
  <verify>corepack pnpm --filter @lottery/web typecheck</verify>
  <done>Role restrictions are enforced consistently across UI and server boundaries.</done>
</task>

</tasks>

<verification>
- `corepack pnpm --filter @lottery/web typecheck`
- role guard smoke scenarios for `user` and `admin` access attempts
</verification>

<success_criteria>

- [ ] Admin-only actions are blocked for `user` role sessions
- [ ] User-only paths reject unauthenticated or invalid sessions
- [ ] Guard logic is centralized, not duplicated in pages
</success_criteria>
