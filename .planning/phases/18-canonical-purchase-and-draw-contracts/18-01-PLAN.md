---
phase: 18-canonical-purchase-and-draw-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/domain/src/purchase-request.ts
  - packages/domain/src/request-state.ts
  - packages/domain/src/draw.ts
  - packages/domain/src/ticket.ts
  - packages/domain/src/terminal-attempt.ts
  - packages/domain/src/index.ts
  - packages/application/src/index.ts
  - packages/application/src/ports/canonical-purchase-store.ts
  - packages/application/src/ports/canonical-draw-store.ts
  - packages/application/src/ports/purchase-attempt-store.ts
  - packages/infrastructure/src/postgres/postgres-schema.ts
  - packages/infrastructure/src/postgres/postgres-purchase-store.ts
  - packages/infrastructure/src/index.ts
  - ARCHITECTURE.md
  - .planning/phases/18-canonical-purchase-and-draw-contracts/18-01-SUMMARY.md
autonomous: true
must_haves:
  truths:
    - canonical purchase state is separate from result state and result visibility
    - canonical draw lifecycle is explicit and additive
    - purchase attempts are durable records, not only journal-note strings
    - no legacy table or write-model is deleted in Phase 18
  artifacts:
    - packages/domain/src/request-state.ts
    - packages/domain/src/draw.ts
    - packages/infrastructure/src/postgres/postgres-schema.ts
  key_links:
    - packages/application/src/services/purchase-orchestration-service.ts
    - packages/application/src/services/terminal-execution-attempt-service.ts
    - .planning/phases/18-canonical-purchase-and-draw-contracts/18-CONTEXT.md
---

<objective>
Add the canonical runtime truth skeleton: domain contracts, additive Postgres schema, and invariant coverage for `purchase`, `draw`, and `purchase_attempt`, without breaking the current Big 8 operating contour.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-canonical-purchase-and-draw-contracts/18-CONTEXT.md
@ARCHITECTURE.md
@packages/application/src/services/purchase-orchestration-service.ts
@packages/application/src/services/terminal-execution-attempt-service.ts
@packages/infrastructure/src/postgres/postgres-schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define canonical domain contracts for purchase, draw, and purchase_attempt</name>
  <files>packages/domain/src/purchase-request.ts, packages/domain/src/request-state.ts, packages/domain/src/draw.ts, packages/domain/src/ticket.ts, packages/domain/src/terminal-attempt.ts, packages/domain/src/index.ts, packages/application/src/index.ts, packages/application/src/ports/canonical-purchase-store.ts, packages/application/src/ports/canonical-draw-store.ts, packages/application/src/ports/purchase-attempt-store.ts</files>
  <read_first>packages/domain/src/purchase-request.ts, packages/domain/src/request-state.ts, packages/domain/src/draw.ts, packages/domain/src/ticket.ts, packages/domain/src/terminal-attempt.ts</read_first>
  <action>Introduce explicit canonical types and helpers: `CanonicalPurchaseRecord`, `CanonicalPurchaseStatus`, `PurchaseResultStatus`, `CanonicalDrawRecord`, `CanonicalDrawStatus`, and `PurchaseAttemptRecord`. Add transition helpers for `submitted -> queued -> processing -> purchased -> awaiting_draw_close -> settled`, error branches for retryable and final failure, and result visibility as a separate axis from execution state. Keep legacy exports compiling, but mark `TicketRecord` as compatibility/read-model territory rather than the business write truth.</action>
  <verify>corepack pnpm --filter @lottery/domain test; corepack pnpm --filter @lottery/application test</verify>
  <acceptance_criteria>
    - `packages/domain/src/request-state.ts` contains canonical purchase states distinct from result visibility
    - `packages/domain/src/draw.ts` contains `open`, `closed`, and `settled` draw lifecycle states
    - `packages/domain/src/terminal-attempt.ts` exports explicit purchase-attempt structures instead of only formatting journal-note text
  </acceptance_criteria>
  <done>Canonical runtime truth exists at the contract level without deleting legacy types.</done>
</task>

<task type="auto">
  <name>Task 2: Add additive Postgres schema and canonical repository skeleton</name>
  <files>packages/infrastructure/src/postgres/postgres-schema.ts, packages/infrastructure/src/postgres/postgres-purchase-store.ts, packages/infrastructure/src/index.ts, packages/application/src/ports/canonical-purchase-store.ts, packages/application/src/ports/canonical-draw-store.ts, packages/application/src/ports/purchase-attempt-store.ts</files>
  <read_first>packages/infrastructure/src/postgres/postgres-schema.ts, packages/infrastructure/src/postgres/postgres-purchase-store.ts</read_first>
  <action>Add new additive tables and repository support without touching legacy tables: `lottery_purchases` with canonical purchase payload/status/result fields, `lottery_draws` with canonical draw lifecycle fields, and `lottery_purchase_attempts` with durable attempt payload and raw terminal response. Keep `lottery_purchase_requests`, `lottery_purchase_queue_items`, `lottery_tickets`, `lottery_ticket_verification_jobs`, `lottery_draw_closures`, and `lottery_terminal_execution_locks` intact in this phase.</action>
  <verify>corepack pnpm --filter @lottery/infrastructure typecheck</verify>
  <acceptance_criteria>
    - `packages/infrastructure/src/postgres/postgres-schema.ts` contains `create table if not exists lottery_purchases`
    - `packages/infrastructure/src/postgres/postgres-schema.ts` contains `create table if not exists lottery_draws`
    - `packages/infrastructure/src/postgres/postgres-schema.ts` contains `create table if not exists lottery_purchase_attempts`
    - infrastructure typecheck exits 0 after the canonical repository skeleton is added
  </acceptance_criteria>
  <done>Additive canonical storage groundwork exists beside the legacy storage model.</done>
</task>

<task type="auto">
  <name>Task 3: Add invariant coverage and update architecture anchors</name>
  <files>packages/domain/src/__tests__/request-state.test.ts, packages/domain/src/__tests__/terminal-attempt.test.ts, packages/application/src/__tests__/purchase-orchestration-service.test.ts, ARCHITECTURE.md, .planning/phases/18-canonical-purchase-and-draw-contracts/18-01-SUMMARY.md</files>
  <read_first>ARCHITECTURE.md, packages/domain/src/__tests__/request-state.test.ts, packages/application/src/__tests__/purchase-orchestration-service.test.ts</read_first>
  <action>Add tests that lock the new invariants: canonical purchase state transitions, canonical draw lifecycle transitions, additive schema expectations, and preservation of legacy compatibility during Phase 18. Update `ARCHITECTURE.md` so the documented truth model explicitly distinguishes canonical write models from temporary compatibility projections.</action>
  <verify>corepack pnpm --filter @lottery/domain test; corepack pnpm --filter @lottery/application test; corepack pnpm --filter @lottery/infrastructure typecheck</verify>
  <acceptance_criteria>
    - domain tests cover canonical purchase/draw transition helpers
    - architecture doc describes canonical write models versus compatibility projections
    - no acceptance criterion or summary claims legacy removal happened in Phase 18
  </acceptance_criteria>
  <done>Phase 18 is guarded by executable invariants and explicit architecture documentation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm --filter @lottery/domain test` passes
- [ ] `corepack pnpm --filter @lottery/application test` passes
- [ ] `corepack pnpm --filter @lottery/infrastructure typecheck` passes
- [ ] `corepack pnpm --filter @lottery/web build` still passes after additive groundwork
</verification>

<output>
After completion, create `.planning/phases/18-canonical-purchase-and-draw-contracts/18-01-SUMMARY.md`
</output>
