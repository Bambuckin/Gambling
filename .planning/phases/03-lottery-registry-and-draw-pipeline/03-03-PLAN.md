---
phase: 03-lottery-registry-and-draw-pipeline
plan: 03
type: execute
wave: 1
depends_on: ["03-02"]
files_modified:
  - packages/domain/src/draw.ts
  - packages/application/src/ports/draw-store.ts
  - packages/application/src/services/draw-refresh-service.ts
  - packages/application/src/__tests__/draw-refresh-service.test.ts
  - packages/infrastructure/src/draw/
  - apps/web/src/lib/draw/
  - apps/web/src/app/lottery/[lotteryCode]/page.tsx
autonomous: true
requirements: [DRAW-01, DRAW-02, DRAW-03, DRAW-04]
user_setup: []
---

<objective>
Implement draw refresh ingestion and freshness model, then enforce stale/missing draw gating in the user flow.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-lottery-registry-and-draw-pipeline/03-CONTEXT.md
@.planning/phases/03-lottery-registry-and-draw-pipeline/03-02-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add draw store contract and freshness orchestration service</name>
  <files>packages/domain/src/draw.ts, packages/application/src/ports/draw-store.ts, packages/application/src/services/draw-refresh-service.ts</files>
  <read_first>packages/domain/src/draw.ts, packages/application/src/ports/time-source.ts, .planning/REQUIREMENTS.md</read_first>
  <action>Create draw storage port and draw refresh service that persists snapshots per lottery, computes freshness windows, and exposes stale/missing state in a deterministic contract.</action>
  <verify>Select-String -Path packages/application/src/services/draw-refresh-service.ts -Pattern "class DrawRefreshService|isFresh|stale"</verify>
  <acceptance_criteria>
    - draw store port exists with per-lottery snapshot operations
    - draw refresh service computes freshness and stale-state outputs
  </acceptance_criteria>
  <done>Draw freshness core is available for UI and purchase gating.</done>
</task>

<task type="auto">
  <name>Task 2: Add in-memory draw adapter and integration tests</name>
  <files>packages/infrastructure/src/draw/, packages/application/src/__tests__/draw-refresh-service.test.ts</files>
  <read_first>packages/infrastructure/src/index.ts, packages/application/src/__tests__/lottery-registry-service.test.ts</read_first>
  <action>Implement in-memory draw snapshot adapter and tests covering fresh, stale, and missing draw states, including purchase-blocking signals when data is stale or absent.</action>
  <verify>corepack pnpm --filter @lottery/application test</verify>
  <acceptance_criteria>
    - draw adapter persists snapshots per lottery code
    - tests cover fresh/stale/missing and blocking outcomes
  </acceptance_criteria>
  <done>Draw freshness behavior is verified at application level.</done>
</task>

<task type="auto">
  <name>Task 3: Surface draw freshness in lottery UI and block purchase submit</name>
  <files>apps/web/src/lib/draw/, apps/web/src/app/lottery/[lotteryCode]/page.tsx</files>
  <read_first>apps/web/src/app/lottery/[lotteryCode]/page.tsx, apps/web/src/lib/access/entry-flow.ts, .planning/research/PITFALLS.md</read_first>
  <action>Show current draw data, freshness timestamp, and clear stale/missing badges on lottery page. Disable purchase initiation controls when draw data is stale or absent.</action>
  <verify>corepack pnpm --filter @lottery/web build</verify>
  <acceptance_criteria>
    - lottery page visibly marks fresh vs stale/missing draw state
    - purchase action controls are blocked when draw state is not actionable
  </acceptance_criteria>
  <done>User flow now enforces draw freshness before purchase initiation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm --filter @lottery/application test` passes
- [ ] `corepack pnpm --filter @lottery/web build` passes
- [ ] stale/missing draw state blocks purchase initiation
</verification>

<output>
After completion, create `.planning/phases/03-lottery-registry-and-draw-pipeline/03-03-SUMMARY.md`
</output>
