---
phase: 03-lottery-registry-and-draw-pipeline
plan: 01
type: execute
wave: 1
depends_on: ["02-05"]
files_modified:
  - packages/domain/src/lottery-registry.ts
  - packages/application/src/ports/lottery-registry-store.ts
  - packages/application/src/services/lottery-registry-service.ts
  - packages/application/src/__tests__/lottery-registry-service.test.ts
  - packages/application/src/index.ts
  - packages/application/package.json
  - packages/infrastructure/src/registry/in-memory-lottery-registry-store.ts
  - packages/infrastructure/src/index.ts
  - apps/web/src/lib/access/lottery-catalog.ts
  - apps/web/src/lib/access/entry-flow.ts
  - apps/web/src/lib/registry/registry-runtime.ts
  - apps/web/src/app/page.tsx
  - apps/web/src/app/debug/registry-lab/page.tsx
  - docs/modules/boundary-catalog.md
  - .planning/codebase/STRUCTURE.md
autonomous: true
requirements: [LOTR-01, LOTR-04]
user_setup: []
must_haves:
  truths:
    - "Lottery registry is a typed source of truth for visibility, ordering, and handler references"
    - "Shell lottery listing reads enabled lotteries from registry service, not static fallback arrays"
    - "A separate debug UI contour exists for manual registry verification after core implementation"
  artifacts:
    - path: "packages/application/src/services/lottery-registry-service.ts"
      provides: "Registry orchestration for list/get/upsert/reorder/visibility operations"
      contains: "class LotteryRegistryService"
    - path: "packages/infrastructure/src/registry/in-memory-lottery-registry-store.ts"
      provides: "In-memory adapter implementing registry storage port"
      contains: "class InMemoryLotteryRegistryStore"
    - path: "apps/web/src/lib/registry/registry-runtime.ts"
      provides: "Runtime composition for registry service in web app"
      contains: "getLotteryRegistryService"
    - path: "apps/web/src/app/debug/registry-lab/page.tsx"
      provides: "Manual UI verification contour for registry state"
      contains: "Registry Lab"
  key_links:
    - from: "apps/web/src/lib/access/lottery-catalog.ts"
      to: "packages/application/src/services/lottery-registry-service.ts"
      via: "shell listing is sourced from registry service"
    - from: "packages/infrastructure/src/registry/in-memory-lottery-registry-store.ts"
      to: "packages/application/src/ports/lottery-registry-store.ts"
      via: "adapter implements storage port"
---

<objective>
Implement Phase 3 first executable slice: registry storage and orchestration with ordering, visibility, and handler references wired into shell reads.

Purpose: make lottery availability deterministic and centrally managed before dynamic forms and draw freshness layers.
Output: registry domain/application/infrastructure slice + shell integration + debug verification UI.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-lottery-registry-and-draw-pipeline/03-CONTEXT.md
@docs/modules/boundary-catalog.md
@.planning/codebase/STRUCTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define registry domain helpers, application port, and service</name>
  <files>packages/domain/src/lottery-registry.ts, packages/application/src/ports/lottery-registry-store.ts, packages/application/src/services/lottery-registry-service.ts, packages/application/src/index.ts</files>
  <read_first>packages/domain/src/lottery-registry.ts, packages/application/src/ports/identity-store.ts, packages/application/src/services/access-service.ts, .planning/REQUIREMENTS.md</read_first>
  <action>Expand registry domain definitions with helper utilities for normalization and deterministic ordering. Add `LotteryRegistryStore` application port with operations to list, find by code, and replace/save entries. Implement `LotteryRegistryService` that enforces unique codes, deterministic ordering, visibility filtering, and stable handler reference presence for each entry. Export the new port and service from package index.</action>
  <verify>Select-String -Path packages/application/src/services/lottery-registry-service.ts -Pattern "class LotteryRegistryService|getVisibleLotteries|upsertLottery"; Select-String -Path packages/application/src/ports/lottery-registry-store.ts -Pattern "interface LotteryRegistryStore"</verify>
  <acceptance_criteria>
    - `LotteryRegistryService` exists and exposes visible-list and upsert/reorder operations
    - `LotteryRegistryStore` port exists under `packages/application/src/ports/`
    - registry entries require both purchase and result handler binding references
  </acceptance_criteria>
  <done>Core registry orchestration exists outside runtime apps and is ready for adapters.</done>
</task>

<task type="auto">
  <name>Task 2: Implement in-memory registry adapter and service tests</name>
  <files>packages/infrastructure/src/registry/in-memory-lottery-registry-store.ts, packages/infrastructure/src/index.ts, packages/application/src/__tests__/lottery-registry-service.test.ts, packages/application/package.json</files>
  <read_first>packages/infrastructure/src/index.ts, packages/test-kit/src/fake-lottery-handler.ts, packages/application/src/__tests__/access-service.test.ts, packages/application/package.json</read_first>
  <action>Create an in-memory registry adapter implementing the new storage port, seeded by initial entries and supporting replace/upsert updates with stable ordering. Add application-level tests that validate: enabled-only listing order, disabled entry exclusion, unique-code enforcement, and handler-reference persistence. Update application test script to run all application tests.</action>
  <verify>corepack pnpm --filter @lottery/application test</verify>
  <acceptance_criteria>
    - in-memory registry adapter compiles and is exported from infrastructure index
    - application tests include registry visibility and ordering scenarios
    - `corepack pnpm --filter @lottery/application test` passes
  </acceptance_criteria>
  <done>Registry core behavior is executable and covered by automated checks.</done>
</task>

<task type="auto">
  <name>Task 3: Wire shell catalog to registry service and add debug Registry Lab UI</name>
  <files>apps/web/src/lib/registry/registry-runtime.ts, apps/web/src/lib/access/lottery-catalog.ts, apps/web/src/lib/access/entry-flow.ts, apps/web/src/app/page.tsx, apps/web/src/app/debug/registry-lab/page.tsx</files>
  <read_first>apps/web/src/lib/access/lottery-catalog.ts, apps/web/src/lib/access/entry-flow.ts, apps/web/src/app/page.tsx, apps/web/src/app/debug/access-lab/page.tsx</read_first>
  <action>Add a registry runtime factory for the web app using the in-memory adapter and deterministic seed entries. Replace shell catalog reads with registry-service reads so home page shows only enabled lotteries in display-order sequence. Ensure lottery entry flow validates requested code against registry and falls back safely when missing/disabled. Add `/debug/registry-lab` page that renders full registry table (enabled flag, display order, form schema, handler refs) as a separate manual verification contour.</action>
  <verify>corepack pnpm --filter @lottery/web build</verify>
  <acceptance_criteria>
    - home page lottery list is sourced via registry service and respects enabled/order
    - registry lab route exists and shows complete registry metadata for manual checks
    - web build passes after integration changes
  </acceptance_criteria>
  <done>Shell behavior is now registry-driven and manually verifiable through dedicated debug UI.</done>
</task>

<task type="auto">
  <name>Task 4: Update module and structure docs for the registry slice</name>
  <files>docs/modules/boundary-catalog.md, .planning/codebase/STRUCTURE.md</files>
  <read_first>docs/modules/boundary-catalog.md, .planning/codebase/STRUCTURE.md, packages/application/src/services/lottery-registry-service.ts, apps/web/src/lib/registry/registry-runtime.ts</read_first>
  <action>Document ownership and integration points for registry service/runtime wiring, plus new debug verification contour location and purpose. Keep boundaries explicit: shell consumes service; service owns logic; adapter owns storage implementation.</action>
  <verify>Select-String -Path docs/modules/boundary-catalog.md -Pattern "registry|Registry Lab"; Select-String -Path .planning/codebase/STRUCTURE.md -Pattern "lottery-registry-service|registry-runtime|registry-lab"</verify>
  <acceptance_criteria>
    - boundary catalog references registry service ownership and debug verification contour
    - structure map includes new registry runtime and adapter files
  </acceptance_criteria>
  <done>Docs and code remain aligned for phase continuation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- **TB-03-01-A:** Registry mutation inputs -> stored handler references and visibility state
- **TB-03-01-B:** Registry read path -> web shell routing and lottery entry eligibility

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| 03-01-T1 | Tampering | Registry storage | mitigate | Validate normalized codes, enforce unique code keys, and preserve deterministic ordering |
| 03-01-T2 | Elevation of Privilege | Handler references | mitigate | Keep handler fields as explicit string bindings; do not execute dynamic input |
| 03-01-T3 | Repudiation | Visibility/order behavior | mitigate | Add service tests and registry lab UI snapshot visibility for manual verification |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm --filter @lottery/application test` passes
- [ ] `corepack pnpm typecheck` passes
- [ ] `corepack pnpm --filter @lottery/web build` passes
- [ ] Home page consumes registry-driven enabled/order list
- [ ] Registry Lab route renders registry metadata
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `03-02` can build dynamic form rendering on top of a stable registry source instead of static shell data
</success_criteria>

<output>
After completion, create `.planning/phases/03-lottery-registry-and-draw-pipeline/03-01-SUMMARY.md`
</output>
