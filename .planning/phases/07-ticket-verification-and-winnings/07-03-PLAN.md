---
phase: 07-ticket-verification-and-winnings
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - packages/domain/src/ticket.ts
  - packages/domain/src/__tests__/ticket.test.ts
  - packages/application/src/services/ticket-verification-result-service.ts
  - packages/application/src/services/wallet-ledger-service.ts
  - packages/application/src/__tests__/ticket-verification-result-service.test.ts
  - packages/application/src/__tests__/wallet-ledger-service.test.ts
  - packages/application/src/index.ts
  - docs/modules/boundary-catalog.md
  - docs/runbooks/ticket-persistence-verification.md
autonomous: true
requirements: [TICK-03, BAL-04]
user_setup: []
must_haves:
  truths:
    - ticket verification stores normalized status, raw output, and winning amount
    - winning credit is idempotent by ticket verification event
  artifacts:
    - packages/application/src/services/ticket-verification-result-service.ts
    - packages/application/src/services/wallet-ledger-service.ts
  key_links:
    - packages/domain/src/ticket.ts
---

<objective>
Normalize verification results and apply winnings to ledger with idempotent references.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ticket-verification-and-winnings/07-CONTEXT.md
@.planning/phases/07-ticket-verification-and-winnings/07-02-SUMMARY.md
@packages/application/src/services/wallet-ledger-service.ts
@packages/domain/src/ledger.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ticket verification result normalization in domain</name>
  <files>packages/domain/src/ticket.ts, packages/domain/src/__tests__/ticket.test.ts</files>
  <read_first>packages/domain/src/ticket.ts</read_first>
  <action>Add ticket helper for verification update that accepts terminal raw output and normalized winning amount, then transitions verification state (`verified|failed`) with immutable update and validation (winning amount must be zero-or-positive integer when present).</action>
  <verify>corepack pnpm --filter @lottery/domain test -- ticket</verify>
  <acceptance_criteria>
    - domain helper rejects negative winning amount
    - verification update sets `verifiedAt` timestamp on completed states
  </acceptance_criteria>
  <done>Ticket domain can safely apply verification outcomes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ticket verification result service</name>
  <files>packages/application/src/services/ticket-verification-result-service.ts, packages/application/src/__tests__/ticket-verification-result-service.test.ts, packages/application/src/index.ts</files>
  <read_first>packages/application/src/ports/ticket-store.ts, packages/application/src/services/ticket-verification-queue-service.ts</read_first>
  <action>Create service that consumes verification terminal output, updates ticket record verification fields, and returns normalized result DTO with `verificationStatus`, `winningAmountMinor`, and `rawOutput`. Ensure replay-safe behavior by skipping duplicate already-verified updates with same verification event key.</action>
  <verify>corepack pnpm --filter @lottery/application test -- ticket-verification-result-service</verify>
  <acceptance_criteria>
    - service updates ticket raw output and winning amount in one save operation
    - replay of same verification event returns `replayed=true`
  </acceptance_criteria>
  <done>Application layer owns verification result normalization and idempotent ticket state updates.</done>
</task>

<task type="auto">
  <name>Task 3: Credit winnings via wallet ledger service with ticket idempotency key</name>
  <files>packages/application/src/services/wallet-ledger-service.ts, packages/application/src/__tests__/wallet-ledger-service.test.ts, docs/modules/boundary-catalog.md, docs/runbooks/ticket-persistence-verification.md</files>
  <read_first>packages/application/src/services/wallet-ledger-service.ts, packages/domain/src/ledger.ts</read_first>
  <action>Add winnings credit method usage path that writes immutable `credit` entries linked to `ticketId` and uses idempotency key `${ticketId}:winnings:${verificationEventId}`. Update tests for duplicate credit suppression and document ownership/verification notes in boundary catalog and runbook.</action>
  <verify>corepack pnpm --filter @lottery/application test -- wallet-ledger-service ticket-verification-result-service</verify>
  <acceptance_criteria>
    - ledger credit entry references `ticketId`
    - duplicate verification event does not create duplicate credit entry
    - boundary catalog references ticket-to-ledger credit boundary
  </acceptance_criteria>
  <done>Winning verification outcomes now produce auditable and idempotent balance credits.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm --filter @lottery/domain test -- ticket` passes
- [ ] `corepack pnpm --filter @lottery/application test -- ticket-verification-result-service wallet-ledger-service` passes
</verification>

<output>
After completion, create `.planning/phases/07-ticket-verification-and-winnings/07-03-SUMMARY.md`
</output>
