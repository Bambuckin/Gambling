---
phase: 07-ticket-verification-and-winnings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/domain/src/ticket.ts
  - packages/domain/src/__tests__/ticket.test.ts
  - packages/domain/src/index.ts
  - packages/application/src/ports/ticket-store.ts
  - packages/application/src/services/ticket-persistence-service.ts
  - packages/application/src/services/terminal-execution-attempt-service.ts
  - packages/application/src/__tests__/ticket-persistence-service.test.ts
  - packages/application/src/__tests__/terminal-execution-attempt-service.test.ts
  - packages/application/src/index.ts
  - packages/infrastructure/src/purchase/in-memory-ticket-store.ts
  - packages/infrastructure/src/index.ts
  - apps/terminal-worker/src/main.ts
  - docs/modules/boundary-catalog.md
  - docs/runbooks/ticket-persistence-verification.md
autonomous: true
requirements: [TICK-01]
user_setup: []
must_haves:
  truths:
    - each request reaching success has exactly one ticket record
    - ticket record includes requestId, userId, lotteryCode, drawId, purchasedAt, and externalReference
  artifacts:
    - packages/application/src/services/ticket-persistence-service.ts
    - packages/application/src/services/terminal-execution-attempt-service.ts
    - packages/application/src/ports/ticket-store.ts
  key_links:
    - apps/terminal-worker/src/main.ts
---

<objective>
Persist ticket records and connect ticket creation to successful purchase outcomes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ticket-verification-and-winnings/07-CONTEXT.md
@packages/application/src/services/terminal-execution-attempt-service.ts
@apps/terminal-worker/src/main.ts
@packages/domain/src/ticket.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand ticket domain model for persisted purchase baseline</name>
  <files>packages/domain/src/ticket.ts, packages/domain/src/__tests__/ticket.test.ts, packages/domain/src/index.ts</files>
  <read_first>packages/domain/src/ticket.ts, packages/domain/src/purchase-request.ts</read_first>
  <action>Add ticket domain helper `createPurchasedTicketRecord(input)` that validates request linkage and initializes verification fields. Persist fields: `ticketId`, `requestId`, `userId`, `lotteryCode`, `drawId`, `purchasedAt`, `externalReference`, `purchaseStatus`, `verificationStatus`, `verificationRawOutput`, `winningAmountMinor`, and `verifiedAt`. Keep defaults deterministic (`purchaseStatus="purchased"`, `verificationStatus="pending"`, no winning amount yet). Add tests for success path and invalid input rejection.</action>
  <verify>corepack pnpm --filter @lottery/domain test -- ticket</verify>
  <acceptance_criteria>
    - `packages/domain/src/ticket.ts` exports `createPurchasedTicketRecord`
    - created ticket defaults to `purchaseStatus="purchased"` and `verificationStatus="pending"`
    - `packages/domain/src/__tests__/ticket.test.ts` contains invalid input coverage
  </acceptance_criteria>
  <done>Domain owns deterministic ticket-record creation with future verification fields pre-shaped.</done>
</task>

<task type="auto">
  <name>Task 2: Add ticket persistence port, service, and in-memory adapter</name>
  <files>packages/application/src/ports/ticket-store.ts, packages/application/src/services/ticket-persistence-service.ts, packages/application/src/__tests__/ticket-persistence-service.test.ts, packages/application/src/index.ts, packages/infrastructure/src/purchase/in-memory-ticket-store.ts, packages/infrastructure/src/index.ts</files>
  <read_first>packages/application/src/ports/purchase-request-store.ts, packages/infrastructure/src/purchase/in-memory-purchase-request-store.ts</read_first>
  <action>Create `TicketStore` port with `listTickets`, `getTicketByRequestId`, and `saveTicket`. Implement `TicketPersistenceService.persistSuccessfulPurchaseTicket(input)` that writes exactly one ticket per successful request (idempotent by `requestId`), deriving `ticketId` as `${requestId}:ticket` and fallback external reference as `${lotteryCode}-${requestId}` when terminal reference is absent. Add in-memory adapter and tests for create + replay behavior.</action>
  <verify>corepack pnpm --filter @lottery/application test -- ticket-persistence-service</verify>
  <acceptance_criteria>
    - `packages/application/src/ports/ticket-store.ts` exists with three methods
    - `persistSuccessfulPurchaseTicket` returns `replayed=true` on duplicate request success
    - `packages/infrastructure/src/purchase/in-memory-ticket-store.ts` persists and returns cloned records
  </acceptance_criteria>
  <done>Application and infrastructure layers can persist ticket records behind a stable port boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Wire ticket persistence into terminal success flow and docs</name>
  <files>packages/application/src/services/terminal-execution-attempt-service.ts, packages/application/src/__tests__/terminal-execution-attempt-service.test.ts, apps/terminal-worker/src/main.ts, docs/modules/boundary-catalog.md, docs/runbooks/ticket-persistence-verification.md</files>
  <read_first>packages/application/src/services/terminal-execution-attempt-service.ts, apps/terminal-worker/src/main.ts, docs/modules/boundary-catalog.md</read_first>
  <action>Inject `TicketPersistenceService` into terminal attempt flow. When normalized outcome is `success`, persist ticket after request transition save and before returning result. Extend worker result mapping to carry `externalTicketReference` from handler output into attempt record input. Update attempt-service tests to assert ticket persistence on success and no ticket persistence for retry/error. Update boundary catalog and add a dedicated runbook for Phase 7 ticket persistence verification.</action>
  <verify>corepack pnpm --filter @lottery/application test -- terminal-execution-attempt-service; corepack pnpm --filter @lottery/terminal-worker typecheck</verify>
  <acceptance_criteria>
    - success-path attempt test asserts one persisted ticket linked to request/draw
    - retry/error attempt tests assert zero persisted tickets
    - `docs/modules/boundary-catalog.md` mentions ticket persistence ownership in application layer
  </acceptance_criteria>
  <done>Successful terminal purchase outcomes now produce persistent ticket records through service boundaries.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm --filter @lottery/domain test -- ticket` passes
- [ ] `corepack pnpm --filter @lottery/application test -- ticket-persistence-service terminal-execution-attempt-service` passes
- [ ] `corepack pnpm --filter @lottery/terminal-worker typecheck` passes
- [ ] `Select-String -Path docs/modules/boundary-catalog.md -Pattern "ticket persistence|TicketStore"` matches
</verification>

<output>
After completion, create `.planning/phases/07-ticket-verification-and-winnings/07-01-SUMMARY.md`
</output>
