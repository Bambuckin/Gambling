---
phase: 07-ticket-verification-and-winnings
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - packages/domain/src/ticket.ts
  - packages/domain/src/__tests__/ticket.test.ts
  - packages/application/src/ports/ticket-verification-job-store.ts
  - packages/application/src/services/ticket-verification-queue-service.ts
  - packages/application/src/__tests__/ticket-verification-queue-service.test.ts
  - packages/application/src/index.ts
  - packages/infrastructure/src/purchase/in-memory-ticket-verification-job-store.ts
  - packages/infrastructure/src/index.ts
  - apps/terminal-worker/src/main.ts
  - docs/modules/boundary-catalog.md
autonomous: true
requirements: [TICK-02]
user_setup: []
must_haves:
  truths:
    - verification can be started once and proceed without manual per-ticket steps
    - verification queue targets only purchased tickets pending verification
  artifacts:
    - packages/application/src/services/ticket-verification-queue-service.ts
    - packages/application/src/ports/ticket-verification-job-store.ts
  key_links:
    - apps/terminal-worker/src/main.ts
---

<objective>
Implement deterministic ticket verification trigger/queue flow through terminal worker boundaries.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ticket-verification-and-winnings/07-CONTEXT.md
@.planning/phases/07-ticket-verification-and-winnings/07-01-SUMMARY.md
@packages/application/src/services/purchase-execution-queue-service.ts
@apps/terminal-worker/src/main.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add verification job domain shape and pending eligibility rules</name>
  <files>packages/domain/src/ticket.ts, packages/domain/src/__tests__/ticket.test.ts</files>
  <read_first>packages/domain/src/ticket.ts</read_first>
  <action>Add domain-level verification job model with statuses `queued|verifying|done|error` and helper that selects tickets eligible for verification (`purchaseStatus=purchased`, `verificationStatus=pending`). Add tests for eligibility filtering.</action>
  <verify>corepack pnpm --filter @lottery/domain test -- ticket</verify>
  <acceptance_criteria>
    - ticket domain exports verification-job status type
    - test covers exclusion of already verified or failed tickets
  </acceptance_criteria>
  <done>Domain defines deterministic verification target eligibility.</done>
</task>

<task type="auto">
  <name>Task 2: Build verification queue service and storage port</name>
  <files>packages/application/src/ports/ticket-verification-job-store.ts, packages/application/src/services/ticket-verification-queue-service.ts, packages/application/src/__tests__/ticket-verification-queue-service.test.ts, packages/application/src/index.ts, packages/infrastructure/src/purchase/in-memory-ticket-verification-job-store.ts, packages/infrastructure/src/index.ts</files>
  <read_first>packages/application/src/ports/ticket-store.ts, packages/application/src/services/purchase-execution-queue-service.ts</read_first>
  <action>Create verification-job store and queue service that enqueues pending tickets exactly once, reserves next verification job with exclusive status transition (`queued -> verifying`), and supports completion/error transitions for retry routing in later plans.</action>
  <verify>corepack pnpm --filter @lottery/application test -- ticket-verification-queue-service</verify>
  <acceptance_criteria>
    - queue service enqueues only pending tickets
    - reserving next job cannot return the same job twice while status is `verifying`
    - tests include idempotent re-enqueue behavior
  </acceptance_criteria>
  <done>Application layer can feed verification jobs to worker flow deterministically.</done>
</task>

<task type="auto">
  <name>Task 3: Extend worker loop to process verification jobs</name>
  <files>apps/terminal-worker/src/main.ts, docs/modules/boundary-catalog.md</files>
  <read_first>apps/terminal-worker/src/main.ts, packages/application/src/services/ticket-verification-queue-service.ts, packages/application/src/services/terminal-handler-resolver-service.ts</read_first>
  <action>Add secondary worker branch that polls verification queue after purchase queue pass, resolves lottery result handler binding, and records verification attempt metadata for each verification job. Update boundary catalog to document that worker now owns verification-job polling while result normalization remains application-owned.</action>
  <verify>corepack pnpm --filter @lottery/terminal-worker typecheck; corepack pnpm --filter @lottery/application test -- ticket-verification-queue-service</verify>
  <acceptance_criteria>
    - worker typecheck passes with verification polling branch
    - boundary catalog includes verification queue ownership note
    - verification queue tests still pass after worker integration
  </acceptance_criteria>
  <done>Verification flow can start and proceed through worker loop without manual per-ticket execution steps.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `corepack pnpm --filter @lottery/domain test -- ticket` passes
- [ ] `corepack pnpm --filter @lottery/application test -- ticket-verification-queue-service` passes
- [ ] `corepack pnpm --filter @lottery/terminal-worker typecheck` passes
</verification>

<output>
After completion, create `.planning/phases/07-ticket-verification-and-winnings/07-02-SUMMARY.md`
</output>
